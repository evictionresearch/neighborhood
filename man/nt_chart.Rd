% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_chart.R
\name{nt_chart}
\alias{nt_chart}
\title{Interactive editorial chart (ERN house style)}
\usage{
nt_chart(
  data,
  x,
  y,
  group = NULL,
  type = c("line", "area", "bar"),
  stack = NULL,
  baseline = NULL,
  baseline_label = NULL,
  band = NULL,
  band_label = NULL,
  palette = NULL,
  value_fmt = c("comma", "percent", "currency", "multiple", "none"),
  smooth = NULL,
  crosshair = TRUE,
  yaxis = c("always", "hover", "none"),
  highlight_last = FALSE,
  end_label = FALSE,
  line_styles = NULL,
  source = NULL,
  legend = !is.null(group),
  title = NULL,
  subtext = NULL,
  x_title = NULL,
  y_title = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame (one row per x value, or per x × group).}

\item{x}{Name of the column for the x-axis (a date, year, or category).}

\item{y}{Name of the numeric column to plot.}

\item{group}{Optional column name; one series per group (e.g. race, county).}

\item{type}{One of \code{"line"}, \code{"area"}, or \code{"bar"}.}

\item{stack}{Optional group key to stack bars/areas (pass a string, commonly
the same as \code{group} or \code{"total"}).}

\item{baseline}{Optional numeric y value for a dashed reference line.}

\item{baseline_label}{Optional label for the baseline.}

\item{band}{Optional length-2 vector \code{c(start, end)} (same type as \code{x}) to
shade a vertical band.}

\item{band_label}{Optional label for the band.}

\item{palette}{Series colors; defaults to the ERN palette.}

\item{value_fmt}{How to format values on the y-axis, the hover crosshair, and
the tooltip (all kept in agreement): one of \code{"comma"} (default), \code{"percent"},
\code{"currency"}, \code{"multiple"}, or \code{"none"}. \strong{\code{"percent"} expects proportions in
\verb{[0, 1]}} (e.g. \code{0.25} renders as \verb{25\%}), matching \code{\link[=nt_popup]{nt_popup()}} and
\code{scales::percent()}. \code{"multiple"} renders a ratio as e.g. \verb{1.52×} (useful for
a parity/disparity chart).}

\item{smooth}{Logical; smooth lines/areas (default \code{TRUE} for line/area,
\code{FALSE} for bars).}

\item{crosshair}{Logical; the hover crosshair + on-axis value (default
\code{TRUE}).}

\item{yaxis}{When the y-axis scale is shown: \code{"always"} (default; labels
always visible), \code{"hover"} (the "newspaper graphic" style used in the ERN
state profiles — the y-axis labels and dashed gridlines are hidden until the
reader hovers the chart, then appear), or \code{"none"} (never; rely on the
hover crosshair value alone).}

\item{highlight_last}{Logical; mark the most recent point in the accent color.
With a \code{group}, the leading series (largest latest value) is accented.}

\item{end_label}{Logical; draw a direct label at the end of each line — the
series name and its latest value (e.g. \code{"Black · 33.5"}) in the series color.
This is the editorial alternative to a legend, so the legend is hidden by
default when \code{end_label = TRUE}. Line/area charts only.}

\item{line_styles}{Optional character vector of dash styles recycled across
series — any of \code{"solid"}, \code{"dashed"}, \code{"dotted"} — for print- and
grayscale-friendly differentiation. If the vector is named, names are matched
to series (group) names; otherwise styles are applied in series order.}

\item{source}{Optional short attribution string shown small in the lower-right
corner (e.g. a data-source credit line).}

\item{legend}{Logical; show a legend (defaults to \code{TRUE} when \code{group} is set,
and to \code{FALSE} when \code{end_label = TRUE}).}

\item{title, subtext}{Optional chart title and subtitle.}

\item{x_title, y_title}{Optional axis titles.}

\item{height}{Optional CSS height (e.g. \code{"380px"}).}

\item{...}{Passed to the underlying series function
(\code{\link[echarts4r:e_line]{echarts4r::e_line()}} / \code{\link[echarts4r:e_bar]{echarts4r::e_bar()}} / \code{\link[echarts4r:e_area]{echarts4r::e_area()}}).}
}
\value{
An \code{echarts4r}/\code{htmlwidget} object.
}
\description{
Builds an interactive line, area, or bar chart in the Eviction Research
Network "newspaper graphic" style — the look of the Washington and Minnesota
state-profile charts. Hovering reveals the scale: a crosshair tracks the
cursor and \strong{the value is printed on the y-axis} where you point, alongside a
tooltip. The axes are otherwise minimal (no chart-junk), so the data leads.

It is a thin, opinionated wrapper over the
\href{https://echarts4r.john-coene.com/}{echarts4r} package (by John Coene), which
bundles Apache ECharts. As with the mapping functions, it returns the
underlying \code{echarts4r} widget, so you can keep piping \verb{echarts4r::e_*}
functions to customize anything it doesn't expose. Charts are \code{htmlwidgets}:
they display in RStudio, Quarto/R Markdown, and Shiny, and save to standalone
HTML with \code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}.
}
\details{
The signature interaction — a crosshair that draws to the axes and shows the
value on the y-axis on hover — is ECharts' \code{axisPointer} of \code{type = "cross"},
enabled by \code{crosshair = TRUE} (the default). Editorial touches map to
arguments:
\itemize{
\item \code{baseline} draws a dashed horizontal reference line (e.g. a pre-pandemic
average, or a \code{1.0} parity line on a ratio chart), like the state-profile
charts.
\item \code{band} shades a vertical region between two x values (e.g. an eviction
moratorium window).
\item \code{highlight_last} marks the most recent point in the accent color (for a
grouped chart, the leading series — the one with the largest latest value).
\item \code{end_label} writes a direct label at the end of each line ("Black · 33.5"),
the editorial alternative to a legend.
\item \code{line_styles} varies the dash pattern per series for print/grayscale
legibility, and \code{source} prints a small attribution in the lower-right —
together these reproduce the look of the published profile plates.
}
}
\examples{
\dontshow{if (interactive() && requireNamespace("echarts4r", quietly = TRUE)) withAutoprint(\{ # examplesIf}
filings <- data.frame(
  month   = seq(as.Date("2019-01-01"), by = "month", length.out = 60),
  filings = round(1000 + 200 * sin(seq_len(60) / 6))
)

# Monthly trend with a pre-pandemic baseline and a moratorium band
nt_chart(filings, "month", "filings", type = "line",
         baseline = mean(filings$filings[1:12]), baseline_label = "Pre-2020 avg",
         band = c(as.Date("2020-03-01"), as.Date("2021-06-01")),
         band_label = "Moratorium", highlight_last = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_spark]{nt_spark()}} for inline sparklines;
\code{\link[echarts4r:e_charts]{echarts4r::e_charts()}} and \code{\link[echarts4r:e_tooltip]{echarts4r::e_tooltip()}} underneath.

Other ern_charts:
\code{\link[=nt_spark]{nt_spark()}}
}
\concept{ern_charts}
