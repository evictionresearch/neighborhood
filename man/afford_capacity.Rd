% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/afford_verdict.R
\name{afford_capacity}
\alias{afford_capacity}
\title{Free-will capacity: units and people, per tenure and tier}
\usage{
afford_capacity(x)
}
\arguments{
\item{x}{An \code{\link[=afford_index]{afford_index()}} result (one row per tract x tenure x tier;
\code{sf} geometry is dropped). Works on banded (\code{\link[=afford_bands]{afford_bands()}}) input too,
where each row set is a non-overlapping slice.}
}
\value{
A tibble, one row per tenure x tier: \code{n_tracts}, \code{tier_households},
\code{affordable_units}, \code{per100_affordable}, \code{shortfall} (positive =
shortage), plus -- when the columns are present -- the stretch-line
(\code{affordable_stretch_units}, \code{per100_stretch}), availability
(\code{open_per_year}, \code{per100_open_year}, \code{open_now}, \code{per100_open_now},
\code{tracts_lt1_open}), and entry-price (\code{\link[=afford_entry]{afford_entry()}}:
\code{affordable_entry_units}, \code{per100_entry}, \code{open_entry_per_year},
\code{per100_open_entry_year}) readouts.
}
\description{
The units-and-people arithmetic for an \code{\link[=afford_index]{afford_index()}} result: per tenure x
tier, how many units the tier's households could rent/own at the standard
burden (\code{affordable_units}, and per 100 tier households), how many of those
actually open up (\code{open_per_year} from turnover, \code{open_now} from vacancy),
and the regional \strong{shortfall} (tier households minus affordable units --
the deficit that remains \emph{even if everyone could re-sort freely}). This is
the NLIHC-Gap-style readout ("X affordable homes per 100 households"),
before any competition adjustment.
}
\details{
Demand (\code{tier_households} = \code{reg_hh_tier}) is \strong{regional}: if you filter
\code{x} to a subset of tracts first, the result reads "the tracts kept could
absorb X per 100 of the \emph{region's} tier households," which is usually what
you want (e.g. capacity of the stable set). Counts are not yet
competition-adjusted -- units affordable to the tier but occupied by
higher-income households still count, so these are ceilings (NLIHC's
"affordable and available" adjustment is the planned refinement).
}
\examples{
\dontrun{
idx <- afford_index("53", "033", 2024, tenure = "rent")
afford_capacity(idx)                                  # region-wide, all tiers
afford_capacity(idx[idx$ami_tier == "VLI", ])         # the pilot group
}
}
\seealso{
\code{\link[=afford_index]{afford_index()}}, \code{\link[=afford_verdict]{afford_verdict()}}
}
