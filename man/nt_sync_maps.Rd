% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_sync.R
\name{nt_sync_maps}
\alias{nt_sync_maps}
\title{Lay out MapLibre maps side by side with synchronized zoom and pan}
\usage{
nt_sync_maps(
  ...,
  titles = NULL,
  height = "520px",
  gap = "10px",
  switcher = NULL
)
}
\arguments{
\item{...}{mapgl/MapLibre htmlwidgets (two or more), or a single list of
them.}

\item{titles}{Optional character vector, one short title per panel,
shown above each map.}

\item{height}{CSS height of the row of maps (default \code{"520px"}).}

\item{gap}{CSS gap between panels (default \code{"10px"}).}

\item{switcher}{Optional layer switcher shared by every panel: a
segmented control above the maps that flips between layer \emph{groups}
across all panels at once (e.g. one layer per AMI tier from
\code{\link[=afford_map]{afford_map()}}\code{(tier = c(...))}). A list:
\describe{
\item{\code{layers}}{Named character vector, \verb{c(<layer_id> = "Button label", ...)}. Clicking a button sets that layer id visible (and the
others hidden) on every panel where the id exists — panels missing
an id are left alone, so a panel whose surface doesn't vary between
groups (a single-layer map) simply never changes.}
\item{\code{active}}{Layer id that starts selected (default the first).}
\item{\code{label}}{Optional short label to the left of the buttons.}
\item{\code{notes}}{Optional named character vector (by layer id) of HTML
shown to the right of the buttons, updated on switch.}
}
Legends bound to a layer (\code{nt_add_choropleth(id = , ...)} ties them via
\code{data-layer-id}) show and hide with it.}
}
\value{
An \code{\link[htmltools:tagList]{htmltools::tagList()}}. Save it as a page with
\code{\link[htmltools:save_html]{htmltools::save_html()}} (dependencies land in a \verb{lib/} folder next to
the file), or print it in an interactive session to preview.
}
\description{
Places two or more \pkg{mapgl} MapLibre widgets in a flex row and wires
their movement together: pan or zoom any panel and the others follow.
The small-multiples pattern for comparing surfaces over the same
geography (e.g. rent change / income change / renter-share change).

mapgl's own \code{\link[mapgl:compare]{mapgl::compare()}} handles exactly two maps (swipe or sync);
this helper takes any number. Each widget's MapLibre \code{Map} object is
reached through the DOM element mapgl binds it to (\code{el.map}), so no
plugin is needed.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# m1, m2, m3: mapgl widgets over the same area
# htmltools::save_html(
#   nt_sync_maps(m1, m2, m3, titles = c("Rent", "Income", "Renters")),
#   "market_maps/index.html"
# )
# Tiered panels + a switcher (see afford_map(tier = c("ELI","VLI","LI"))):
# nt_sync_maps(m1, m2, m3, switcher = list(
#   label  = "Household income",
#   layers = c(afford_ELI = "ELI", afford_VLI = "VLI", afford_LI = "LI"),
#   active = "afford_VLI"))
\dontshow{\}) # examplesIf}
}
