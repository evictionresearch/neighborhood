% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_layers.R
\name{nt_layers_control}
\alias{nt_layers_control}
\title{Add a layer switcher for multi-layer maps}
\usage{
nt_layers_control(
  map,
  layers,
  labels = NULL,
  title = NULL,
  position = c("bottom-right", "bottom-left", "top-left", "top-right")
)
}
\arguments{
\item{map}{A map with two or more layers added via \code{\link[=nt_add_choropleth]{nt_add_choropleth()}}
(each with a distinct \code{id}).}

\item{layers}{Character vector of layer \code{id}s to switch between, in order. The
first is shown initially; the rest should have been added with
\code{visible = FALSE}.}

\item{labels}{Optional display labels for the control (defaults to \code{layers}).}

\item{title}{Optional title shown above the options.}

\item{position}{One of \code{"bottom-right"} (default), \code{"bottom-left"},
\code{"top-left"}, or \code{"top-right"}. The default keeps the switcher clear of the
legend (top-left) and the navigation/search controls (top-right).}
}
\value{
The map, with the layer switcher added.
}
\description{
Adds a compact radio control that switches between choropleth layers added
with \code{\link[=nt_add_choropleth]{nt_add_choropleth()}}, showing \strong{one layer at a time} and revealing only
that layer's legend. This is the clean way to put several measures on a single
map — for example neighborhood typology plus the share of each demographic
group — and let the reader flip between them.

Add the layers first: give each its own \code{id}, and set \code{visible = FALSE} on all
but the first. Then call \code{nt_layers_control()} with those ids.
}
\details{
MapLibre lets a map hold many layers at once; this control just toggles their
visibility (and the matching legends) so they don't pile on top of one
another. It is a small, self-contained piece of JavaScript attached with
\code{\link[htmlwidgets:onRender]{htmlwidgets::onRender()}} — deterministic, and it reads/sets layer state
through the standard MapLibre API. For a raw multi-select (checkbox) toggle
instead, use \code{\link[mapgl:add_layers_control]{mapgl::add_layers_control()}}.
}
\examples{
\dontshow{if (interactive() && requireNamespace("mapgl", quietly = TRUE)) withAutoprint(\{ # examplesIf}
md <- ntdf(state = "MD", county = "Baltimore City", geometry = TRUE)

# Typology + the share of each demographic group, switchable on one map
demos <- c(White = "pWhite", Black = "pBlack", Asian = "pAsian", Latine = "pLatine")
m <- nt_maplibre(md) |>
  nt_add_choropleth(md, "nt_conc", id = "Typology", legend_title = "Neighborhood type")
for (i in seq_along(demos)) {
  m <- nt_add_choropleth(m, md, demos[[i]], id = names(demos)[i],
                         legend_title = paste("Share", names(demos)[i]),
                         visible = FALSE)
}
nt_layers_control(m, layers = c("Typology", names(demos)), title = "Show")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}} (use \code{visible = FALSE} for the hidden layers);
\code{\link[mapgl:add_layers_control]{mapgl::add_layers_control()}} for a raw checkbox toggle.

Other ern_maps:
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}},
\code{\link[=nt_add_labels]{nt_add_labels()}},
\code{\link[=nt_map]{nt_map()}},
\code{\link[=nt_maplibre]{nt_maplibre()}},
\code{\link[=nt_pmtiles]{nt_pmtiles()}}
}
\concept{ern_maps}
