% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_rank.R
\name{nt_bar}
\alias{nt_bar}
\title{Ranked / horizontal bar chart (ERN house style)}
\usage{
nt_bar(
  data,
  x,
  y,
  group = NULL,
  orientation = c("horizontal", "vertical"),
  sort = c("desc", "asc", "none"),
  n = NULL,
  highlight = NULL,
  diverging = FALSE,
  baseline = NULL,
  baseline_label = NULL,
  palette = NULL,
  value_fmt = c("comma", "percent", "currency", "multiple", "none"),
  value_labels = TRUE,
  legend = TRUE,
  yaxis = c("always", "hover", "none"),
  bar_width = NULL,
  title = NULL,
  subtext = NULL,
  source = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame: one row per category (or per category × group).}

\item{x}{Name of the category column.}

\item{y}{Name of the numeric value column.}

\item{group}{Optional column; one (dodged) series per group.}

\item{orientation}{\code{"horizontal"} (default) or \code{"vertical"}.}

\item{sort}{\code{"desc"} (default), \code{"asc"}, or \code{"none"} — order categories by
value. Ignored when \code{group} is set.}

\item{n}{Optional integer; keep only the top \code{n} categories after sorting.}

\item{highlight}{Which bars to accent. \code{NULL} (default) accents the rank
leader; \code{FALSE} accents none; a character vector accents categories by
name; a numeric vector accents those row positions. Ignored when \code{group}
or \code{diverging} is set.}

\item{diverging}{Logical; color bars by their sign relative to \code{baseline}
(above = accent, below = navy) and draw a dashed reference line. Good for
"above/below average" charts.}

\item{baseline}{Optional numeric reference value (the divider for
\code{diverging}, or just a dashed reference line otherwise).}

\item{baseline_label}{Optional label for the reference line.}

\item{palette}{Bar colors (defaults to the ERN palette). For grouped bars,
one color per group (a named vector matches by group name).}

\item{value_fmt}{How to format values on the axis, hover crosshair, tooltip,
and bar labels: \code{"comma"} (default), \code{"percent"} (expects \verb{[0,1]}),
\code{"currency"}, \code{"multiple"} (\verb{1.52×}), or \code{"none"}.}

\item{value_labels}{Logical; print each bar's value at its end (default
\code{TRUE}; suppressed for grouped bars).}

\item{legend}{Logical; show the series legend (only applies to grouped bars;
default \code{TRUE}).}

\item{yaxis}{When the value-axis scale shows: \code{"always"} (default), \code{"hover"}
(the newspaper style — hidden until the reader hovers), or \code{"none"}. The
value axis always starts at zero (filled bars are honest about area).}

\item{bar_width}{Optional bar width (e.g. \code{"62\%"} or a pixel number).}

\item{title, subtext}{Optional chart title and subtitle.}

\item{source}{Optional attribution shown small in the lower-right.}

\item{height}{Optional CSS height (e.g. \code{"420px"}).}

\item{...}{Passed to \code{\link[echarts4r:e_bar]{echarts4r::e_bar()}}.}
}
\value{
An \code{echarts4r}/\code{htmlwidget} object.
}
\description{
A categorical bar chart in the Eviction Research Network "newspaper graphic"
style — the look of the "top counties by filing rate" and "rate by race"
plates. Bars are ranked and (by default) horizontal so long category names
read cleanly; the leading bar is accented, each bar is labelled with its
value, and the value-axis scale can hide until the reader hovers
(\code{yaxis = "hover"}).

Like \code{\link[=nt_chart]{nt_chart()}}, it returns the underlying \code{echarts4r} widget, so you can
keep piping \verb{echarts4r::e_*} functions to customize anything it doesn't
expose.
}
\details{
Three common forms fall out of the arguments:
\itemize{
\item \strong{Ranked bars} (the default): \code{sort = "desc"}, optionally \code{n} to keep the
top N, with the leader accented. Set \code{highlight} to accent specific
categories instead (e.g. \code{highlight = "Black"} on a rate-by-race chart).
\item \strong{Diverging bars}: \code{diverging = TRUE} colors each bar by whether it sits
above or below \code{baseline} (e.g. above/below the statewide average), and
draws a dashed reference line there.
\item \strong{Grouped bars}: pass \code{group} for side-by-side (dodged) bars — e.g. rate
by race \emph{within} each county.
}
}
\examples{
\dontshow{if (interactive() && requireNamespace("echarts4r", quietly = TRUE)) withAutoprint(\{ # examplesIf}
counties <- data.frame(
  county = c("Hennepin", "Ramsey", "Anoka", "Dakota", "Olmsted"),
  rate   = c(42.3, 38.1, 19.4, 22.7, 15.2)
)
nt_bar(counties, "county", "rate", value_fmt = "comma")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_lollipop]{nt_lollipop()}} for the dot-plot variant; \code{\link[=nt_chart]{nt_chart()}} for trends.

Other ern_charts:
\code{\link[=ern_palette]{ern_palette()}},
\code{\link[=ern_swatch]{ern_swatch()}},
\code{\link[=nt_chart]{nt_chart()}},
\code{\link[=nt_dumbbell]{nt_dumbbell()}},
\code{\link[=nt_lollipop]{nt_lollipop()}},
\code{\link[=nt_pd_bart]{nt_pd_bart()}},
\code{\link[=nt_pd_plot]{nt_pd_plot()}},
\code{\link[=nt_range]{nt_range()}},
\code{\link[=nt_scatter]{nt_scatter()}},
\code{\link[=nt_slope]{nt_slope()}},
\code{\link[=nt_spark]{nt_spark()}},
\code{\link[=nt_stacked_bar]{nt_stacked_bar()}},
\code{\link[=nt_waffle]{nt_waffle()}},
\code{\link{scale_ern}},
\code{\link[=theme_ern]{theme_ern()}}
}
\concept{ern_charts}
