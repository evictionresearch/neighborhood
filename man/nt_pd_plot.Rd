% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_pd.R
\name{nt_pd_plot}
\alias{nt_pd_plot}
\title{Partial-dependence plot with a frequency panel (ERN house style)}
\usage{
nt_pd_plot(
  pd,
  observed = NULL,
  var_label = NULL,
  y_label = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  bins = 30,
  rel_heights = c(4, 1),
  points = TRUE,
  effect = c("response", "probit")
)
}
\arguments{
\item{pd}{A data frame of the PD curve with columns \code{x} (the predictor value
at each evaluation point), \code{mean} (posterior-mean partial effect), and
\code{lower}/\code{upper} (credible-interval bounds).}

\item{observed}{Numeric vector of the predictor's observed (training) values,
used to draw the frequency histogram beneath the curve. If \code{NULL}, only the
PD curve is returned (no frequency panel).}

\item{var_label}{X-axis label (the human-readable predictor name). Defaults to
\code{"x"}.}

\item{y_label}{Y-axis label for the PD curve. Defaults to \code{"Partial effect"}.}

\item{title, subtitle, caption}{Optional editorial text.}

\item{bins}{Number of histogram bins for the frequency panel (default \code{30}).}

\item{rel_heights}{Relative heights of the PD panel vs. the frequency panel
(default \code{c(4, 1)}).}

\item{points}{Logical; draw a point at each PD evaluation quantile (default
\code{TRUE}), echoing the CHEST-Lab/Brinkmann style.}

\item{effect}{Either \code{"response"} (default y-label "Partial effect") or
\code{"probit"} (classification models; y-label "Partial effect (probit)"). Only
affects the default y-label.}
}
\value{
A \code{patchwork} object (PD panel stacked over the frequency panel) when
\code{observed} is supplied, otherwise a single \code{ggplot2} object. Either is
printable and \code{ggplot2::ggsave()}-able.
}
\description{
Draws a partial-dependence (PD) curve — the posterior-mean partial effect of
one predictor with a credible ribbon — over a histogram of that predictor's
observed values. The histogram (the "frequency table underneath") anchors the
curve to the data density, so readers discount the wiggly tails where the
model has little support. This is the figure form used for the HPRM EDR/EER
driver plots.

The function is model-agnostic: it takes a tidy table of PD points plus the
raw predictor values, so it can plot partial dependence from any model. For
the HPRM \code{bartMachine} models, \code{\link[=nt_pd_bart]{nt_pd_bart()}} computes that table for you.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE) && requireNamespace("patchwork", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
x  <- sort(c(rnorm(400, 2, 0.6), rnorm(120, 4.5, 0.4)))     # observed values
xg <- quantile(x, c(.05, seq(.1, .9, .1), .95))             # evaluation grid
pd <- data.frame(x = xg, mean = 0.3 * xg,
                 lower = 0.3 * xg - 0.15, upper = 0.3 * xg + 0.15)
nt_pd_plot(pd, observed = x, var_label = "Median gross rent",
           title = "Partial dependence: median rent")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_pd_bart]{nt_pd_bart()}} to compute \code{pd}/\code{observed} from a fitted \code{bartMachine}
model; \code{\link[=theme_ern]{theme_ern()}} for the shared print theme.

Other ern_charts:
\code{\link[=ern_palette]{ern_palette()}},
\code{\link[=ern_swatch]{ern_swatch()}},
\code{\link[=nt_bar]{nt_bar()}},
\code{\link[=nt_chart]{nt_chart()}},
\code{\link[=nt_dumbbell]{nt_dumbbell()}},
\code{\link[=nt_lollipop]{nt_lollipop()}},
\code{\link[=nt_pd_bart]{nt_pd_bart()}},
\code{\link[=nt_range]{nt_range()}},
\code{\link[=nt_scatter]{nt_scatter()}},
\code{\link[=nt_slope]{nt_slope()}},
\code{\link[=nt_spark]{nt_spark()}},
\code{\link[=nt_stacked_bar]{nt_stacked_bar()}},
\code{\link[=nt_waffle]{nt_waffle()}},
\code{\link{scale_ern}},
\code{\link[=theme_ern]{theme_ern()}}
}
\concept{ern_charts}
