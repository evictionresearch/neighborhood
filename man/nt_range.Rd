% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_range.R
\name{nt_range}
\alias{nt_range}
\title{Ranked estimate + error-bar (confidence interval) chart (ERN house style)}
\usage{
nt_range(
  data,
  x,
  value,
  error = NULL,
  low = NULL,
  high = NULL,
  sort = c("desc", "asc", "none"),
  n = NULL,
  orientation = c("horizontal", "vertical"),
  highlight = NULL,
  palette = NULL,
  value_fmt = c("comma", "percent", "currency", "multiple", "none"),
  value_labels = TRUE,
  yaxis = c("always", "hover", "none"),
  dot_size = 12,
  title = NULL,
  subtext = NULL,
  source = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame: one row per category.}

\item{x}{Name of the category column.}

\item{value}{Name of the point-estimate column.}

\item{error}{Name of a symmetric +/- column (e.g. an ACS margin of error).
Mutually exclusive with \code{low}/\code{high}.}

\item{low, high}{Names of explicit lower/upper bound columns (use instead of
\code{error}).}

\item{sort}{\code{"desc"} (default), \code{"asc"}, or \code{"none"} — order by the estimate.}

\item{n}{Optional integer; keep only the top \code{n} categories after sorting.}

\item{orientation}{\code{"horizontal"} (default) or \code{"vertical"}.}

\item{highlight}{Which estimate to accent: \code{NULL} (default) accents the rank
leader; \code{FALSE} none; a character vector by category; numeric row positions.}

\item{palette}{Dot colors (defaults to the ERN palette).}

\item{value_fmt}{Value formatting (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{value_labels}{Logical; label each dot with its estimate (default \code{TRUE}).}

\item{yaxis}{Value-axis scale visibility (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{dot_size}{Dot diameter in pixels (default \code{12}).}

\item{title, subtext, source, height}{Standard editorial extras (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{...}{Passed to \code{\link[echarts4r:e_scatter]{echarts4r::e_scatter()}}.}
}
\value{
An \code{echarts4r}/\code{htmlwidget} object.
}
\description{
A ranked dot-and-whisker chart: a dot at each category's estimate with a bar
spanning its uncertainty. Because the Eviction Research Network works almost
entirely on American Community Survey data — where every estimate ships with a
margin of error — showing the interval is the honest way to compare groups
(overlapping whiskers warn against over-reading a gap). Same minimal editorial
style as \code{\link[=nt_lollipop]{nt_lollipop()}}; the leader is accented and the scale can hide until
hover.

Give the uncertainty either as a symmetric \code{error} (the ACS MOE, so the bar is
\verb{value ± error}) or as explicit \code{low}/\code{high} bound columns.
}
\examples{
\dontshow{if (interactive() && requireNamespace("echarts4r", quietly = TRUE)) withAutoprint(\{ # examplesIf}
inc <- data.frame(
  race  = c("White", "Black", "Latine", "Asian"),
  med   = c(78000, 51000, 56000, 92000),
  moe   = c(1800, 3200, 2900, 4100)
)
nt_range(inc, "race", value = "med", error = "moe",
         value_fmt = "currency", highlight = "Black")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_lollipop]{nt_lollipop()}} for a point-only ranking; \code{\link[=nt_dumbbell]{nt_dumbbell()}} for a gap
between two measures.

Other ern_charts:
\code{\link[=ern_palette]{ern_palette()}},
\code{\link[=ern_swatch]{ern_swatch()}},
\code{\link[=nt_bar]{nt_bar()}},
\code{\link[=nt_chart]{nt_chart()}},
\code{\link[=nt_dumbbell]{nt_dumbbell()}},
\code{\link[=nt_lollipop]{nt_lollipop()}},
\code{\link[=nt_pd_bart]{nt_pd_bart()}},
\code{\link[=nt_pd_plot]{nt_pd_plot()}},
\code{\link[=nt_scatter]{nt_scatter()}},
\code{\link[=nt_slope]{nt_slope()}},
\code{\link[=nt_spark]{nt_spark()}},
\code{\link[=nt_stacked_bar]{nt_stacked_bar()}},
\code{\link[=nt_waffle]{nt_waffle()}},
\code{\link{scale_ern}},
\code{\link[=theme_ern]{theme_ern()}}
}
\concept{ern_charts}
