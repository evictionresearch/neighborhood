% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erase_water.R
\name{nt_erase_water}
\alias{nt_erase_water}
\title{Erase water area from an sf layer (coastline / lake cutout)}
\usage{
nt_erase_water(
  x,
  state,
  county = NULL,
  area_min = 5e+05,
  buffer = 1000,
  year = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{sf} object to clip (tracts, places, counties, ...). Attributes
are preserved; only geometry is trimmed.}

\item{state}{Two-letter state abbreviation or FIPS code for the water pull. A
single state.}

\item{county}{Optional county name(s) / FIPS within \code{state}. \code{NULL} (default)
infers the counties from \code{x}'s own buffered footprint, so a small or
one-county input only downloads the water it actually needs.}

\item{area_min}{Minimum water-body area in square meters to erase. Default
\code{5e5} (0.5 km²) — large enough to drop ponds and creeks, small enough to
keep bays, sloughs, large lakes, and the open coast.}

\item{buffer}{Distance (CRS units; meters for an equal-area CRS such as 3310,
or via s2 for lon/lat) to extend the area of interest when selecting and
cutting water, so bodies that lap the edge of \code{x} — including just over a
county line — are still erased. Default \code{1000} (1 km). Larger considers
more nearby water (and may pull an adjacent county).}

\item{year}{Vintage for the water (and county-inference) layers. \code{NULL}
(default) lets tigris choose its default year.}

\item{...}{Passed to \code{\link[tigris:area_water]{tigris::area_water()}}.}
}
\value{
\code{x} with water erased, geometry re-extracted to polygons, in \code{x}'s
original CRS. If no qualifying water is found near \code{x}, \code{x} is returned
unchanged.
}
\description{
Clip open water out of polygon geometry so coastal and bay-front tracts (or
places, counties, any \code{sf}) display as their land footprint instead of a
rectangle that runs out into the ocean. This is the cartographic complement
to \code{\link[=nt_areal_weight]{nt_areal_weight()}}: that function already weights by population and land
area (water blocks carry zero), so the \emph{numbers} never count water — this
makes the \emph{map} agree, by removing the water polygon from the shapes you draw.

It is deliberately lean. Water is fetched (and cut) \strong{only for the area of
interest plus a small \code{buffer}} — never a whole state, let alone the nation:
it selects just the counties \code{x} (buffered) overlaps, and crops each county's
water to the area of interest right after download, so it never holds or
unions an entire county's ocean. The \code{buffer} lets it still erase water that
borders the edge of \code{x}, including in an adjacent county.
}
\examples{
\dontrun{
library(tigris)
tr <- tracts("CA", "San Mateo", cb = TRUE, year = 2020)
tr_land <- nt_erase_water(tr, "CA", "San Mateo")   # bay + Pacific trimmed off
}
}
\seealso{
\code{\link[=nt_areal_weight]{nt_areal_weight()}} for the population/area weighting this
complements; \code{\link[tigris:area_water]{tigris::area_water()}} for the source water geometry.
}
