% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zcta_weights.R
\name{nt_zcta_weights}
\alias{nt_zcta_weights}
\title{Housing-unit weights from ZCTAs to tracts (block-exact)}
\usage{
nt_zcta_weights(state, counties, year = 2020, cb = FALSE, crs = 3857, ...)
}
\arguments{
\item{state}{State (FIPS or name), single.}

\item{counties}{County or counties (FIPS or names).}

\item{year}{Decennial vintage for blocks and ZCTAs (default \code{2020}).}

\item{cb}{\code{FALSE} (default) for full-resolution ZCTAs (exact nesting);
\code{TRUE} for the generalized cartographic file (lighter, approximate at
block edges).}

\item{crs}{Projected CRS for the block-in-ZCTA point assignment (default
\code{3857}). Because blocks nest in ZCTAs, any sane projected CRS gives the
same assignment.}

\item{...}{Passed to \code{\link[tigris:blocks]{tigris::blocks()}}.}
}
\value{
A tibble, one row per (ZCTA, tract) overlap: \code{zcta}, \code{tract_geoid},
in-ZCTA block totals (\code{hu_in}, \code{pop_in}, \code{area_in}), whole-tract totals
(\code{hu_t}, \code{pop_t}, \code{area_t}), and fractions (\code{frac_hu}, \code{frac_pop},
\code{frac_area}). \code{frac_hu} sums to 1 over each tract (up to blocks outside
any ZCTA, which are rare).
}
\description{
For each (ZCTA, tract) overlap in the study counties, the share of the
tract's housing units (population, land area) that falls inside the ZCTA —
computed on 2020 census blocks, which nest within both 2020 tracts and 2020
ZCTAs, so the fractions are exact rather than areal approximations. This is
the crosswalk \code{\link[=afford_entry]{afford_entry()}} uses to bring ZIP-level asking rents (Zillow
ZORI) onto tracts.
}
\details{
The first call downloads the \strong{national} 2020 ZCTA boundary file through
\code{tigris} (it is not published by state) and filters it to the blocks'
bounding box; with \code{options(tigris_use_cache = TRUE)} that download happens
once. By default the full-resolution boundaries are used so block nesting
is exact; \code{cb = TRUE} substitutes the lighter generalized file (small
edge-block misassignment possible).
}
\examples{
\dontrun{
w <- nt_zcta_weights("53", "033")     # King County
# tract-level asking-rent premium = HU-weighted mean of its ZIPs' premiums
}
}
\seealso{
\code{\link[=afford_entry]{afford_entry()}}, \code{\link[=nt_areal_weight]{nt_areal_weight()}}
}
