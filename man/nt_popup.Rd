% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_popup.R
\name{nt_popup}
\alias{nt_popup}
\title{Build HTML popup content for a map}
\usage{
nt_popup(data, fields = NULL, title = NULL, html = NULL, prefix_title = TRUE)
}
\arguments{
\item{data}{A data frame or \code{sf} object. Geometry, if present, is ignored.}

\item{fields}{Optional named vector or list mapping \strong{display label ->
column name}, e.g. \code{c("County" = "county", "Median rent" = "med_rent")}.
Fields render in the order given.}

\item{title}{Optional title shown bold at the top. If \code{title} is the name of
a column in \code{data}, that column's per-row value is used (e.g.
\code{title = "GEOID"} yields a different title per tract); otherwise \code{title} is
treated as a literal string shared by every popup.}

\item{html}{Optional character vector (length 1 or \code{nrow(data)}) of extra
HTML appended after the fields. Use it to graft on bar charts, SVG, links,
etc.}

\item{prefix_title}{Logical; if \code{TRUE} (default) and \code{title} names a column,
the title reads like \code{"Tract: <value>"}. Set \code{FALSE} to show the bare value.}
}
\value{
A character vector of length \code{nrow(data)} containing one HTML string
per feature.
}
\description{
Assembles a character vector of HTML — one entry per row of \code{data} — suitable
for use as the \code{popup} (or \code{tooltip}) argument of \code{\link[=nt_add_choropleth]{nt_add_choropleth()}} or any
\code{mapgl} layer function. MapLibre renders whatever HTML the popup column
contains, so this is the seam where a map grows from a plain label into the
rich, multi-panel popups used in the precarity and state-profile maps.

The output is fully \strong{deterministic}: the same data in produces byte-for-byte
identical HTML out (no timestamps, no random ids), so popups are reproducible
and testable.
}
\details{
Start simple and graft upward (this is exactly the arc the \emph{Mapping with
MapLibre} vignette follows):
\enumerate{
\item \strong{A title and a few fields.} Pass \code{fields} as a named vector mapping the
label you want shown to the column it comes from. Numeric columns are
formatted automatically — values that all fall in \verb{[0, 1]} are shown as
percentages, other numerics get thousands separators.
\item \strong{Custom HTML.} Pass \code{html} (a length-1 or length-\code{nrow(data)} character
vector) to append arbitrary markup after the fields — inline \verb{<div>} bar
charts of racial composition, an SVG "bubble track" comparing a tract to
its county and the nation, and so on. Because it is just a string, you
build it in R with \code{paste0()}/\code{sprintf()} and stay deterministic.
}
}
\examples{
\dontrun{
md <- ntdf(state = "MD", county = "Baltimore City", geometry = TRUE)

# Simple: a per-tract title plus two fields
md$popup <- nt_popup(
  md,
  title  = "GEOID",
  fields = c("Neighborhood type" = "nt_conc", "Total population" = "totraceE")
)

# Rich-ready: graft a racial-composition bar chart on top
bars <- with(md, paste0(
  "<div style='margin-top:6px'>",
  "<div style='background:#1f78b4;height:8px;width:", round(pBlack * 100), "\%'></div>",
  "<div style='background:#33a02c;height:8px;width:", round(pAsian * 100), "\%'></div>",
  "</div>"
))
md$popup <- nt_popup(md, title = "GEOID", fields = c("Type" = "nt_conc"), html = bars)

nt_map(md) # picks up the `popup` column automatically
}
}
\seealso{
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}}, which calls this to build a default popup;
the \emph{Mapping with MapLibre} and \emph{precarity-mapping} vignettes for the full
race-bar / bubble-track popup recipe.
}
