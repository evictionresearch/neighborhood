% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inflow_index.R
\name{inflow_index}
\alias{inflow_index}
\title{Revealed-destination layer: where low-income households are moving to}
\usage{
inflow_index(
  hprm,
  tier = c("overall", "ELI", "VLI", "LI"),
  x = NULL,
  growth_cut = 50
)
}
\arguments{
\item{hprm}{HPRM data: a data.frame with \code{GEOID}/\code{geoid} and the relevant
net-migration fields (\code{dis_value} for \code{"overall"}; \code{nmr_pred_el/vl/l} for a
tier), or a path to a \code{.parquet} (needs \code{arrow}) or \code{.rds}.}

\item{tier}{\code{"overall"} (default, \code{dis_value}) or \code{"ELI"}/\code{"VLI"}/\code{"LI"} (the
matched raw \verb{nmr_pred_*}).}

\item{x}{Optional \code{\link[=afford_index]{afford_index()}} result to join the inflow onto (by
\code{GEOID}) for a potential-vs-revealed comparison. For a tier-specific
comparison, filter \code{x} to the same \code{ami_tier} first (inflow is per-tract and
is broadcast across a tract's rows).}

\item{growth_cut}{Net-migration threshold above which a tract is a confident
in-migration destination (default \code{+50}).}
}
\value{
If \code{x} is \code{NULL}, a data.frame of \code{GEOID}, \code{tier}, \code{inflow} (the
net-migration value), \code{growth} (logical), and \code{inflow_cat}. If \code{x} is
supplied, \code{x} with those columns joined on.
}
\description{
The mirror of \code{\link[=afford_index]{afford_index()}}. Where a group \emph{could} live is affordability;
where it \emph{does} land is revealed by HPRM's BART net-migration predictions.
\code{inflow_index()} reads net migration of low-income households and flags
\strong{growth} tracts -- confident net in-migration, i.e. revealed destinations.
Compare it against \code{\link[=afford_index]{afford_index()}} (potential) and \code{\link[=afford_stability]{afford_stability()}}
(holdable): the gap between where a group can afford to go and where it
actually goes is the economic-segregation signal.
}
\details{
\code{tier} selects the basis (per design):
\itemize{
\item \code{"overall"} uses \strong{\code{dis_value}} -- the low-income composite (positive
tail = "low-income growth", per the HPRM codebook). Best for the overall
picture across ELI/VLI/LI together.
\item \code{"ELI"}/\code{"VLI"}/\code{"LI"} use the \strong{raw \code{nmr_pred_el}/\code{nmr_pred_vl}/
\code{nmr_pred_l}} -- tier-specific net migration ("Negative = net outflow"),
matched to the \code{\link[=afford_index]{afford_index()}} tier. Best when asking about one group.
}
A tract is \code{growth = inflow > growth_cut} (default \code{+50}, HPRM's model-noise
buffer -- below it, net migration can't be told from zero). \code{inflow_cat} bins
this into \code{growth} / \code{break-even} (\code{0} to \code{growth_cut}) / \code{outflow} (\verb{< 0}).

\strong{Descriptive, not normative}: high inflow can mean a group is \emph{channeled}
into the only tracts it can afford, not freely choosing -- read it against
affordability and stability, not alone.

\strong{These are predictions, not observed moves.} \code{dis_value}/\verb{nmr_pred_*} are
BART predictions (Data Axle panel, INLA-smoothed) -- a \emph{modeled expectation}
of net migration, and the model's validated target is \emph{displacement} (net
outflow), so the positive/growth tail is comparatively less validated and is
shrunk toward regional means. Treat inflow as a modeled signal to
\strong{triangulate} against observed migration (ACS geographic-mobility-by-income
B07010; IRS SOI county flows by AGI), not as ground truth. To use an observed
source instead, pass a data.frame with \code{GEOID} and a net-migration column of
the expected name (\code{dis_value} or \verb{nmr_pred_*}).

\strong{Known face-validity failure (positive tail) -- experimental; not a destination
signal.} Mapped for the Bay Area, positive \code{dis_value} reads exclusionary and
receiving suburbs as major low-income destinations (Marin median +338, Sonoma
+448, Contra Costa +400) while the expensive urban cores go negative -- because
EDR is a \emph{displacement} model and its positive tail is unvalidated extrapolation
dominated by spatial smoothing where the low-income base is tiny. \strong{Do not use
positive \code{dis_value} as a standalone growth/destination signal.} For
destinations use observed migration (ACS B07010 / IRS SOI) gated by affordable
capacity; positive \code{dis_value}'s only defensible use is \emph{absence of displacement}
feeding \code{\link[=afford_stability]{afford_stability()}} (which clamps the magnitude away). A purpose-built
growth model is specified in \code{hprm/HPRM_3.0_growth_model.md}. This function is
retained as experimental scaffolding and for the displacement (negative) read.
}
\examples{
\dontrun{
hprm <- "~/data/.../hprm_v5_full_2022.parquet"
inflow_index(hprm)                       # overall low-income destinations (dis_value)
inflow_index(hprm, tier = "VLI")         # where VLI households move (nmr_pred_vl)
# potential vs revealed for VLI renters in King County:
idx <- afford_index("53", "033", 2024, tenure = "rent")
cmp <- inflow_index(hprm, tier = "VLI", x = idx[idx$ami_tier == "VLI", ])
}
}
\seealso{
\code{\link[=afford_index]{afford_index()}}, \code{\link[=afford_stability]{afford_stability()}}
}
