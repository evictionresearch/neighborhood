% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ami_cutoffs.R
\name{hud_ami}
\alias{hud_ami}
\title{Pull the HUD Area Median Income (AMI) by year and location}
\usage{
hud_ami(
  state,
  counties,
  years = 2024,
  ami_source = c("auto", "hud", "hud_acs", "acs_fmr", "acs")
)
}
\arguments{
\item{state}{State (FIPS code like \code{"06"} or a name/abbreviation like \code{"CA"}).}

\item{counties}{County or counties (3-digit FIPS or names).}

\item{years}{One or more years (HUD fiscal year for \code{hud}/\code{hud_acs}; ACS5
endpoint for \code{acs}/\code{acs_fmr}). Default \code{2024}.}

\item{ami_source}{\code{"auto"} (default), \code{"hud"}, \code{"hud_acs"}, \code{"acs_fmr"}, or
\code{"acs"} — see \code{\link[=ami_cutoffs]{ami_cutoffs()}}.}
}
\value{
A tibble, one row per county x year: \code{GEOID}, \code{NAME}, \code{year}, \code{ami},
and \code{ami_source} (the source that actually filled the row).
}
\description{
The standalone AMI puller: one row per county x year with the \strong{AMI level}
HUD anchors its income limits on — no tier ceilings, no joins, just the
number, with provenance. Wraps \code{\link[=ami_cutoffs]{ami_cutoffs()}} (same source ladder, same
county/name handling) but is vectorized over \code{years} and records which
source each row actually came from.
}
\details{
What \code{ami} is depends on the source that filled the row (the \code{ami_source}
column says which): \code{"hud"} is HUD's published area median (family) income
for the fiscal year — the number to quote in reports; \code{"hud_acs"} is the
Census-only reproduction (county \code{B19113} at FY-2, trended); \code{"acs_fmr"} /
\code{"acs"} are the county \code{B19113} median family income at the ACS \code{year}.
Under \code{"auto"} each year resolves independently to the most exact source
available (hud -> hud_acs -> acs_fmr -> acs), so a multi-year pull can mix
sources — filter on \code{ami_source} if that matters for the analysis.

The AMI level is not household-size adjusted (size factors apply to the
\emph{limits}, not the median) — for size-adjusted tier ceilings use
\code{\link[=ami_cutoffs]{ami_cutoffs()}}.
}
\examples{
\dontrun{
hud_ami("53", "033")                          # King County, FY2024, exact HUD
hud_ami("CA", c("San Diego", "San Francisco"), 2024)
hud_ami("06", "073", years = 2019:2024)       # a time series (sources may mix)
hud_ami("06", "073", 2024, ami_source = "acs")  # Census-only, reproducible
}
}
\seealso{
\code{\link[=ami_cutoffs]{ami_cutoffs()}} (tier ceilings), \code{\link[=afford_index]{afford_index()}}
}
