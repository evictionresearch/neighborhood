% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_map.R
\name{nt_map}
\alias{nt_map}
\title{Map neighborhood data in one line}
\usage{
nt_map(data, color = NULL, ...)
}
\arguments{
\item{data}{Polygon spatial data. An \code{sf} object (e.g. from
\code{ntdf(geometry = TRUE)}), \strong{or} a path to a shapefile / GeoJSON /
GeoPackage, an \code{sfc} geometry, or an \code{sp}/\code{terra} object — anything is
normalized to \code{sf}.}

\item{color}{Name of the column to map. Defaults to \code{"nt_conc"} if present.}

\item{...}{Passed to \code{\link[=nt_add_choropleth]{nt_add_choropleth()}} (e.g. \code{type}, \code{breaks}, \code{legend},
\code{popup}).}
}
\value{
A \code{mapgl}/\code{maplibregl} \code{htmlwidget}.
}
\description{
The quickest path to a finished map. \code{nt_map()} initializes an ERN-styled
MapLibre map and draws \code{data} as a choropleth — basemap, fit-to-data, legend,
popup, tooltip, and controls included. It is shorthand for
\code{\link[=nt_maplibre]{nt_maplibre()}} piped into \code{\link[=nt_add_choropleth]{nt_add_choropleth()}}.

When \code{color} is omitted and the data has an \code{nt_conc} column (from \code{\link[=ntdf]{ntdf()}}),
it maps the neighborhood typologies with the canonical palette.

Need more control? Stop using \code{nt_map()} and build the map up yourself with
\code{\link[=nt_maplibre]{nt_maplibre()}} + \code{\link[=nt_add_choropleth]{nt_add_choropleth()}} + \code{\link[=nt_add_labels]{nt_add_labels()}} (+ any \code{mapgl}
function). Same pieces, fully exposed.
}
\examples{
\dontshow{if (interactive() && requireNamespace("mapgl", quietly = TRUE)) withAutoprint(\{ # examplesIf}
md <- ntdf(state = "MD", county = "Baltimore City", geometry = TRUE)
nt_map(md)                       # typologies, one line
nt_map(md, color = "pBlack")     # a numeric column instead
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_maplibre]{nt_maplibre()}}, \code{\link[=nt_add_choropleth]{nt_add_choropleth()}}, \code{\link[=nt_add_labels]{nt_add_labels()}}.

Other ern_maps:
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}},
\code{\link[=nt_add_labels]{nt_add_labels()}},
\code{\link[=nt_maplibre]{nt_maplibre()}},
\code{\link[=nt_pmtiles]{nt_pmtiles()}}
}
\concept{ern_maps}
