% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_map_sync.R
\name{nt_map_sync}
\alias{nt_map_sync}
\title{Synchronize MapLibre maps side by side}
\usage{
nt_map_sync(
  ...,
  group = NULL,
  height = 480,
  gap = 8,
  titles = NULL,
  subtitles = NULL
)
}
\arguments{
\item{...}{Two or more MapLibre maps (\code{mapgl} \code{maplibregl} htmlwidgets),
or a single \code{list()} of them.}

\item{group}{Sync-group id. Default \code{NULL} auto-generates a fresh group per
call (maps sync within the call only). Pass the same string across calls
to sync maps in different rows/sections.}

\item{height}{Height (CSS pixels) applied to every map (default \code{480});
\code{NULL} keeps each widget's own height.}

\item{gap}{Horizontal gap between the maps, in pixels (default \code{8}).}

\item{titles, subtitles}{Optional per-map heading and small grey subheading
shown above each panel; length 1 (recycled) or one per map.}
}
\value{
A browsable \code{htmltools} tag: the row of synced maps. Print it,
include it in an R Markdown chunk, or embed it with other tags.
}
\description{
Lays two or more MapLibre maps out in a row and keeps their views locked
together: pan, zoom, or rotate any one and the others follow instantly, so
the same area can be compared across layers at once (before/after, two
engines, afford vs. stability, ...).

Build each map first — \code{\link[=nt_map]{nt_map()}}, \code{\link[=nt_maplibre]{nt_maplibre()}} +
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}}, or any \code{mapgl::maplibre()} pipeline — then hand them
to \code{nt_map_sync()}. Works in R Markdown/Quarto chunks, the RStudio viewer,
and saved HTML alike.
}
\details{
Synchronization is client-side: each map registers into a small per-group
registry when the page loads, and on every \code{move} event copies its camera
(center, zoom, bearing, pitch) to the rest of the group via \code{jumpTo}. A
shared guard flag prevents feedback loops (MapLibre event dispatch is
synchronous). Maps only sync \emph{within} a group: rows built by separate
\code{nt_map_sync()} calls stay independent unless you pass the same \code{group}.

To sync maps that are \strong{not} side by side (e.g. two maps in different
sections of a document), give both calls the same \code{group} — the layout is
per-call, the synchronization is per-group.
}
\examples{
\dontshow{if (interactive() && requireNamespace("mapgl", quietly = TRUE)) withAutoprint(\{ # examplesIf}
md <- ntdf(state = "MD", county = "Baltimore City", geometry = TRUE)
nt_map_sync(
  nt_map(md),                                  # typologies
  nt_map(md, color = "pBlack"),                # \% Black
  titles = c("Neighborhood typologies", "Percent Black")
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_map]{nt_map()}}, \code{\link[=nt_maplibre]{nt_maplibre()}}; \code{mapgl::compare()} for a two-map
swipe/slider comparison.

Other ern_maps:
\code{\link[=afford_map]{afford_map()}},
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}},
\code{\link[=nt_add_labels]{nt_add_labels()}},
\code{\link[=nt_declutter_basemap]{nt_declutter_basemap()}},
\code{\link[=nt_layers_control]{nt_layers_control()}},
\code{\link[=nt_map]{nt_map()}},
\code{\link[=nt_maplibre]{nt_maplibre()}},
\code{\link[=nt_pmtiles]{nt_pmtiles()}}
}
\concept{ern_maps}
