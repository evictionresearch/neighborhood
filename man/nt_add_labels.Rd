% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_labels.R
\name{nt_add_labels}
\alias{nt_add_labels}
\title{Add text labels to a map}
\usage{
nt_add_labels(
  map,
  data,
  text_column,
  id = NULL,
  size = 11,
  color = "#333333",
  halo_color = "#ffffff",
  halo_width = 1.2,
  min_zoom = NULL,
  ...
)
}
\arguments{
\item{map}{A map from \code{\link[=nt_maplibre]{nt_maplibre()}} (or \code{\link[mapgl:maplibre]{mapgl::maplibre()}}).}

\item{data}{Spatial data (\code{sf}, a shapefile/GeoJSON/GeoPackage path, \code{sfc}, or
\code{sp}/\code{terra}; normalized to \code{sf}). Polygons are reduced to interior label
points; point/line inputs are used as-is.}

\item{text_column}{Name of the column whose values are shown as labels.}

\item{id}{Layer id. Defaults to a name derived from \code{text_column}.}

\item{size}{Text size in pixels (default \code{11}).}

\item{color}{Text color (default dark grey).}

\item{halo_color}{Halo color (default white).}

\item{halo_width}{Halo width in pixels (default \code{1.2}).}

\item{min_zoom}{Optional minimum zoom at which labels appear (handy to avoid
clutter when zoomed out).}

\item{...}{Passed to \code{\link[mapgl:add_symbol_layer]{mapgl::add_symbol_layer()}}.}
}
\value{
The map, with the label layer added.
}
\description{
Places a text label at an interior point of each feature — county names,
place names, or any other column. Labels are drawn with a white halo for
legibility over a choropleth, matching the state-profile maps.

Points are computed with \code{\link[sf:st_point_on_surface]{sf::st_point_on_surface()}} (not a centroid), so the
label always lands inside irregular polygons rather than in a bay or notch.
}
\examples{
\dontshow{if (interactive() && requireNamespace("mapgl", quietly = TRUE)) withAutoprint(\{ # examplesIf}
md <- ntdf(state = "MD", county = "Baltimore City", geometry = TRUE)
nt_maplibre(md) |>
  nt_add_choropleth(md, "nt_conc") |>
  nt_add_labels(md, "NAME", min_zoom = 10)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}}; \code{\link[mapgl:add_symbol_layer]{mapgl::add_symbol_layer()}} underneath.

Other ern_maps:
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}},
\code{\link[=nt_layers_control]{nt_layers_control()}},
\code{\link[=nt_map]{nt_map()}},
\code{\link[=nt_maplibre]{nt_maplibre()}},
\code{\link[=nt_pmtiles]{nt_pmtiles()}}
}
\concept{ern_maps}
