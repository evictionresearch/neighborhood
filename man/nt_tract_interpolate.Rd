% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_tract_interpolate.R
\name{nt_tract_interpolate}
\alias{nt_tract_interpolate}
\title{Weight tract-level estimates onto a place boundary}
\usage{
nt_tract_interpolate(
  x,
  weights,
  place = NULL,
  persons = NULL,
  households = NULL,
  areas = NULL,
  id = "GEOID",
  suffix = ""
)
}
\arguments{
\item{x}{A tract-level data frame (or \code{sf}; geometry is ignored) with an
\code{id} column of tract GEOIDs and the estimate columns to scale.}

\item{weights}{A weights table from \code{\link[=nt_areal_weight]{nt_areal_weight()}} — or any data frame
with \code{tract_geoid}, \code{place_name}, and the \verb{frac_*} columns (e.g. the
\code{hprm_tract_weights.csv} audit trail a pipeline has already verified).}

\item{place}{Optional place name(s) to keep from \code{weights} (matched against
\code{place_name}). \code{NULL} keeps every place present.}

\item{persons, households, areas}{Character vectors naming the columns of \code{x}
to scale by \code{frac_pop}, \code{frac_hu}, and \code{frac_area} respectively. Columns
not named in any of the three pass through unscaled (ids, labels, scores).}

\item{id}{Name of the tract-id column in \code{x} (default \code{"GEOID"}).}

\item{suffix}{Appended to the scaled columns' names (default \code{""}, i.e.
scaled in place). Pass e.g. \code{"_wt"} to keep the raw columns alongside.}
}
\value{
A tibble with one row per (place, tract): the \code{weights} columns
(\code{place_name}, \code{tract_geoid}, \verb{frac_*}, ...) joined to \code{x}, with the named
columns scaled to their in-place share. Tracts of \code{x} that touch no kept
place are dropped; tracts in \code{weights} missing from \code{x} are kept with \code{NA}
estimates (so a coverage gap is visible, not silent).
}
\description{
The apply side of \code{\link[=nt_areal_weight]{nt_areal_weight()}}: scale tract-level counts down to the
share that falls inside a place, so jurisdiction statistics describe only
the people (or households) within the boundary. Person counts scale by
\code{frac_pop}, household counts by \code{frac_hu}, land measures by \code{frac_area} —
match the fraction to the variable's unit.

Weighted counts are non-integer by construction (88\% of 1,000 people is
880, but 88\% of 1,043 is 917.8); compute shares from them directly, and
round only for display.
}
\examples{
\dontrun{
w <- nt_areal_weight("CA", "San Mateo", places = "Redwood City")
acs <- tidycensus::get_acs("tract", table = "B05001", state = "CA",
                           county = "San Mateo", year = 2022,
                           output = "wide")
rwc <- nt_tract_interpolate(acs, w, persons = grep("E$", names(acs), value = TRUE))
sum(rwc$B05001_001E)   # persons inside the city limit, not whole-tract sums
}
}
\seealso{
\code{\link[=nt_areal_weight]{nt_areal_weight()}} for building the weights.

Other ern_maps:
\code{\link[=afford_map]{afford_map()}},
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}},
\code{\link[=nt_add_labels]{nt_add_labels()}},
\code{\link[=nt_declutter_basemap]{nt_declutter_basemap()}},
\code{\link[=nt_hprm_map]{nt_hprm_map()}},
\code{\link[=nt_hprm_pal]{nt_hprm_pal()}},
\code{\link[=nt_layers_control]{nt_layers_control()}},
\code{\link[=nt_map]{nt_map()}},
\code{\link[=nt_map_sync]{nt_map_sync()}},
\code{\link[=nt_maplibre]{nt_maplibre()}},
\code{\link[=nt_pmtiles]{nt_pmtiles()}}
}
\concept{ern_maps}
