% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/afford_entry.R
\name{afford_zori}
\alias{afford_zori}
\title{Download and tidy Zillow's ZIP-level asking-rent index (ZORI)}
\usage{
afford_zori(month = NULL, path = NULL, refresh = FALSE, quiet = FALSE)
}
\arguments{
\item{month}{Month column to keep (e.g. \code{"2026-05-31"}); \code{NULL} (default)
uses the latest in the file.}

\item{path}{Optional path to an already-downloaded ZORI csv. Default caches
under \code{tools::R_user_dir("neighborhood", "cache")}.}

\item{refresh}{Re-download even if the cache exists (default \code{FALSE}; the
file updates monthly).}

\item{quiet}{Passed to \code{\link[utils:download.file]{utils::download.file()}}.}
}
\value{
A data.frame: \code{zip}, \code{state}, \code{city}, \code{metro}, \code{county_name},
\code{zori} (typical asking rent, $/month), \code{month}.
}
\description{
Fetches (and caches) Zillow's Observed Rent Index — the smoothed typical
\strong{asking rent} of listed units, ZIP level, monthly — and returns one row
per ZIP for one month. This is the market-entry price signal behind
\code{\link[=afford_entry]{afford_entry()}}: pane 1 prices tracts at what sitting tenants pay; ZORI is
what the door costs.
}
\examples{
\dontrun{
z <- afford_zori()
median(z$zori[z$county_name == "King County" & z$state == "WA"])
}
}
\seealso{
\code{\link[=afford_entry]{afford_entry()}}, \code{\link[=nt_zcta_weights]{nt_zcta_weights()}}
}
