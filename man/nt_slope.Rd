% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_slope.R
\name{nt_slope}
\alias{nt_slope}
\title{Slope chart (ERN house style)}
\usage{
nt_slope(
  data,
  x,
  y,
  group,
  highlight = NULL,
  palette = NULL,
  value_fmt = c("comma", "percent", "currency", "multiple", "none"),
  value_labels = TRUE,
  yaxis = c("none", "hover", "always"),
  title = NULL,
  subtext = NULL,
  source = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame in long form: one row per \code{group} × \code{x}.}

\item{x}{Name of the period column (2+ ordered values, e.g. years).}

\item{y}{Name of the numeric value column.}

\item{group}{Name of the series column (one line each).}

\item{highlight}{Optional \code{group} values to accent (a character vector of
names, or numeric positions); the rest are drawn in a muted steel. If \code{NULL}
or \code{FALSE}, the full ERN palette is applied to every line. (Unlike the
ranked charts, where \code{NULL} accents the leader, a slope chart has no single
leader to accent.)}

\item{palette}{Optional series colors (named vector matched to group names
takes precedence over \code{highlight}).}

\item{value_fmt}{Value formatting for the end labels and hover (see
\code{\link[=nt_bar]{nt_bar()}}).}

\item{value_labels}{Logical; label each line's value at both ends (default
\code{TRUE}).}

\item{yaxis}{Value-axis scale visibility; defaults to \code{"none"} (the values
live in the direct labels, so the scale is usually redundant).}

\item{title, subtext, source, height}{Standard editorial extras (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{...}{Passed to \code{\link[echarts4r:e_line]{echarts4r::e_line()}}.}
}
\value{
An \code{echarts4r}/\code{htmlwidget} object.
}
\description{
A slope (or "parallel coordinates") chart: each \code{group} is a line connecting
its value across the periods on \code{x} — typically two (a before and an after).
Direct labels at the line ends replace the legend, and a highlighted series
can be accented while the rest recede. This is the spare editorial form for
"what changed between 2019 and 2024" comparisons.
}
\examples{
\dontshow{if (interactive() && requireNamespace("echarts4r", quietly = TRUE)) withAutoprint(\{ # examplesIf}
chg <- data.frame(
  place = rep(c("Hennepin", "Ramsey", "Dakota"), each = 2),
  year  = rep(c(2019, 2024), 3),
  rent  = c(1180, 1490, 1090, 1380, 1320, 1610)
)
nt_slope(chg, "year", "rent", group = "place",
         value_fmt = "currency", highlight = "Ramsey")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_chart]{nt_chart()}} for many-period trends; \code{\link[=nt_dumbbell]{nt_dumbbell()}} for gaps.

Other ern_charts:
\code{\link[=ern_palette]{ern_palette()}},
\code{\link[=ern_swatch]{ern_swatch()}},
\code{\link[=nt_bar]{nt_bar()}},
\code{\link[=nt_chart]{nt_chart()}},
\code{\link[=nt_dumbbell]{nt_dumbbell()}},
\code{\link[=nt_lollipop]{nt_lollipop()}},
\code{\link[=nt_pd_bart]{nt_pd_bart()}},
\code{\link[=nt_pd_plot]{nt_pd_plot()}},
\code{\link[=nt_range]{nt_range()}},
\code{\link[=nt_scatter]{nt_scatter()}},
\code{\link[=nt_spark]{nt_spark()}},
\code{\link[=nt_stacked_bar]{nt_stacked_bar()}},
\code{\link[=nt_waffle]{nt_waffle()}},
\code{\link{scale_ern}},
\code{\link[=theme_ern]{theme_ern()}}
}
\concept{ern_charts}
