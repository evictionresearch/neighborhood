% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/afford_verdict.R
\name{afford_verdict}
\alias{afford_verdict}
\title{The three-class affordability verdict (affordable / roughly / not)}
\usage{
afford_verdict(x, share_cut = 0.5)
}
\arguments{
\item{x}{An \code{\link[=afford_index]{afford_index()}} result with \code{supply} and \code{supply_stretch}
(i.e. run with a non-\code{NULL} \code{stretch}; \code{0.50} is the default).}

\item{share_cut}{Share of a tract's stock that must clear a burden line for
the verdict (default \code{0.5} -- the majority / median-unit rule).}
}
\value{
\code{x} with a \code{verdict} factor
(\code{affordable} / \verb{roughly affordable} / \verb{not affordable}; \code{NA} where the
tract has no priced stock). For maps, pair the levels with unambiguous
traffic-light colors, e.g. \verb{#1a9850} / \verb{#fdae61} / \verb{#d73027}.
}
\description{
Classifies each tract x tenure x tier row of an \code{\link[=afford_index]{afford_index()}} result into
a plain-language verdict for the tier's households:
\itemize{
\item \strong{affordable} -- most of the stock is within reach at the standard
burden (default 30\% of income);
\item \strong{roughly affordable} -- not that, but most of the stock is within
reach by \emph{stretching} to the \code{stretch} burden (default 50\%, HUD's
severe-burden threshold);
\item \strong{not affordable} -- even at the stretch line, most of the stock is
out of reach.
}
This is the headline map layer: policy audiences read maps by color, so the
end product renders verdicts (e.g. green / orange / red), with continuous
shares demoted to tooltips or appendices.
}
\details{
At the default \code{share_cut = 0.5} (a tract-majority rule) each class is a
\strong{median-unit sentence}: "the typical rental in this tract would cost a
household at this income no more than 30\% of income" (affordable), "30-50\%"
(roughly), "more than half their income" (not affordable).

The verdict is a \strong{price statement} (Gate 1). It says nothing about whether
an affordable unit is open (\verb{available_*}, Gate 2), whether the household
would be accepted (Gate 3), or whether it could \emph{stay}
(\code{\link[=afford_stability]{afford_stability()}}) -- read it alongside those layers, and see
\code{\link[=afford_capacity]{afford_capacity()}} for the units-and-people arithmetic.
}
\examples{
\dontrun{
idx <- afford_index("53", "033", 2024, tenure = "rent")
v   <- afford_verdict(idx)
table(v$verdict[v$ami_tier == "VLI"])
}
}
\seealso{
\code{\link[=afford_index]{afford_index()}}, \code{\link[=afford_capacity]{afford_capacity()}}, \code{\link[=afford_stability]{afford_stability()}}
}
