% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_pmtiles.R
\name{nt_pmtiles}
\alias{nt_pmtiles}
\title{Build a PMTiles archive from spatial data}
\usage{
nt_pmtiles(
  data,
  path = NULL,
  layer = "nt",
  tippecanoe_args = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{Spatial data: an \code{sf} object, or a path to a shapefile / GeoJSON /
GeoPackage, an \code{sfc}, or an \code{sp}/\code{terra} object (normalized to \code{sf} and
re-projected to WGS84 as needed). All non-geometry columns are carried into
the tiles as feature properties (so popup/tooltip columns survive tiling).}

\item{path}{Output \code{.pmtiles} path. Defaults to a temporary file.}

\item{layer}{Tile layer name (default \code{"nt"}). Must equal the \code{source_layer}
used to draw it.}

\item{tippecanoe_args}{Optional character vector of tippecanoe flags that
\emph{replaces} the defaults (the output \code{-o}, layer \code{-l}, and input are always
set for you).}

\item{quiet}{Logical; suppress tippecanoe's console output (default \code{FALSE}).}
}
\value{
The path to the written \code{.pmtiles} file (invisibly), suitable for
\code{\link[mapgl:add_pmtiles_source]{mapgl::add_pmtiles_source()}}.
}
\description{
Converts an \code{sf} object into a \href{https://docs.protomaps.com/pmtiles/}{PMTiles}
vector-tile archive using the \href{https://github.com/felt/tippecanoe}{\code{tippecanoe}}
command-line tool. PMTiles is how the state-profile maps serve large layers
(the full-US tract file is ~74,000 polygons) fast and from a single static
file — far beyond what can be embedded directly in an HTML page.

Hand the returned path to \code{\link[mapgl:add_pmtiles_source]{mapgl::add_pmtiles_source()}}, or let
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}} do it for you when a layer is large
(\code{tiles = "auto"} / \code{"pmtiles"}).
}
\details{
\code{tippecanoe} is an \strong{optional external dependency} — it is not an R package.
Install it once (for example \verb{brew install tippecanoe} on macOS, or build from
source) and it is picked up from your \code{PATH}. Without it, this function stops
with installation guidance.

The default tippecanoe flags (\verb{-zg --drop-densest-as-needed --extend-zooms-if-still-dropping --force}) auto-select a zoom range and keep
tiles a reasonable size; override them with \code{tippecanoe_args} for full control.
The layer name written into the archive is \code{layer}, which must match the
\code{source_layer} used when adding the layer to a map — \code{\link[=nt_add_choropleth]{nt_add_choropleth()}}
keeps these in lockstep by defaulting both to \code{"nt"}.
}
\examples{
\dontshow{if (interactive() && nzchar(Sys.which("tippecanoe"))) withAutoprint(\{ # examplesIf}
# Tile the full-US typologies (join the bundled table to tract geometry first)
\dontrun{
us <- tigris::tracts(cb = TRUE, year = 2024) |>
  dplyr::left_join(us_nt_tracts2024, by = "GEOID")
tiles <- nt_pmtiles(us, path = "us_typologies.pmtiles")

nt_maplibre() |>
  mapgl::add_pmtiles_source("us", url = tiles, promote_id = "GEOID") |>
  mapgl::add_fill_layer(id = "nt", source = "us", source_layer = "nt",
                        fill_color = "#1f78b4")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}} (which calls this for large layers) and
\code{\link[mapgl:add_pmtiles_source]{mapgl::add_pmtiles_source()}}.

Other ern_maps:
\code{\link[=afford_map]{afford_map()}},
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}},
\code{\link[=nt_add_labels]{nt_add_labels()}},
\code{\link[=nt_declutter_basemap]{nt_declutter_basemap()}},
\code{\link[=nt_hprm_map]{nt_hprm_map()}},
\code{\link[=nt_hprm_pal]{nt_hprm_pal()}},
\code{\link[=nt_layers_control]{nt_layers_control()}},
\code{\link[=nt_map]{nt_map()}},
\code{\link[=nt_map_sync]{nt_map_sync()}},
\code{\link[=nt_maplibre]{nt_maplibre()}},
\code{\link[=nt_tract_interpolate]{nt_tract_interpolate()}}
}
\concept{ern_maps}
