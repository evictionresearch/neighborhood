% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_apartmentlist.R
\name{nt_apartmentlist_rents}
\alias{nt_apartmentlist_rents}
\title{Apartment List rent estimates, always the latest monthly release}
\usage{
nt_apartmentlist_rents(
  dir = "~/data/apartment_list",
  refresh = c("auto", "never", "force"),
  quiet = FALSE
)
}
\arguments{
\item{dir}{Folder holding the CSVs (created if missing). Default
\code{"~/data/apartment_list"}.}

\item{refresh}{\code{"auto"} (default: download only when the site has a newer
vintage than the newest local file), \code{"never"} (offline: newest local
file only), or \code{"force"} (re-download the current release).}

\item{quiet}{Logical; suppress progress messages.}
}
\value{
A data frame of the release, with the vintage string
(\code{"YYYY_MM"}) in \code{attr(, "vintage")}.
}
\description{
Reads the newest \code{Apartment_List_Rent_Estimates_YYYY_MM.csv} in \code{dir} —
and, unless told otherwise, first checks
\url{https://www.apartmentlist.com/research/category/data-rent-estimates}
for a newer monthly release and downloads it. Apartment List links each
release from a CDN; the link is read out of the page source, so no API
key or scraping framework is needed. If the site is unreachable, the
newest local file is used with a message.

The file is the full national release (county, metro, city rows; monthly
columns back to 2017) — filter to the rows you need.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
al <- nt_apartmentlist_rents()
attr(al, "vintage")
\dontshow{\}) # examplesIf}
}
