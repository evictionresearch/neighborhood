% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/afford_stability.R
\name{afford_stability}
\alias{afford_stability}
\title{Stable-destination layer: weight affordable, available supply by HPRM stability}
\usage{
afford_stability(
  x,
  hprm,
  stability_source = c("both", "edr", "eer"),
  edr_weight = 0.5,
  availability = c("turnover", "vacancy", "none"),
  stable_cut = 0.8,
  caution_cut = 0.5
)
}
\arguments{
\item{x}{An \code{\link[=afford_index]{afford_index()}} result (must have \code{GEOID}; for the default
\code{availability}, the \code{available_turnover}/\code{available_vacancy} columns from
\code{afford_index(..., availability = TRUE)}).}

\item{hprm}{The HPRM data: a data.frame with \code{GEOID}/\code{geoid}, \code{dis_value},
\code{ev_value}, or a path to a \code{.parquet} (needs the \code{arrow} package) or \code{.rds}.}

\item{stability_source}{\code{"both"} (default; weighted average), \code{"edr"}
(displacement only), or \code{"eer"} (eviction only).}

\item{edr_weight}{Weight on EDR when \code{stability_source = "both"} (default
\code{0.5}; EER gets \code{1 - edr_weight}).}

\item{availability}{Which afford_index availability measure feeds the chain:
\code{"turnover"} (default, \code{available_turnover}), \code{"vacancy"}
(\code{available_vacancy}), or \code{"none"} (afford-only -- uses \code{accessible},
skipping Gate 2).}

\item{stable_cut}{Stability at/above which a tract counts as a stable
destination (default \code{0.8}, ~ HUD "At Risk or better").}

\item{caution_cut}{Stability at/above which a tract is "elevated" rather than
"precarious" (default \code{0.5}).}
}
\value{
\code{x} with added columns: \code{dis_value}, \code{ev_value}, \code{s_edr}, \code{s_eer},
\code{stability}, \code{eer_out_of_sample}, \code{attainable} (the availability count fed
to the gate), \code{stable_dest} (\code{attainable * stability} -- the continuous
headline), \code{stable} (logical hard filter), and \code{stable_cat} (a
stable/elevated/precarious factor). Re-running on a result that already
carries these columns simply recomputes them (idempotent).
}
\description{
Joins ERN's Housing Precarity Risk Model (HPRM) onto an \code{\link[=afford_index]{afford_index()}}
result and completes the destination chain \strong{afford -> available -> stable}:
of the units a group can afford (\code{\link[=afford_index]{afford_index()}} Gate 1) and that are
actually open (\code{\link[=afford_index]{afford_index()}} Gate 2 \verb{available_*}), how many are in tracts
a low-income household can plausibly \strong{stay} in (low displacement + eviction
risk, Gate 3)? Stability is a \emph{place-level risk descriptor}, not an
individual-outcome claim (the EDR/EER signals are built from individual data
but published as tract rates).
}
\details{
Two HPRM fields drive stability, each mapped to 0-1 on HPRM's \strong{published risk
anchors}:
\itemize{
\item \strong{Displacement} \code{dis_value} (EDR; net out-migration of low-income
renters -- households \emph{leaving}): \code{s_edr = clamp01((dis_value + 300)/350)}
(\code{+50 -> 1.0}, \code{-300} Extreme \verb{-> 0}).
\item \strong{Eviction} \code{ev_value} (EER; filing-rate ratio, the added precarity of
the formal-eviction channel): \code{s_eer = clamp01((2 - ev_value)/1.2)}
(\code{0.8 -> 1.0}, \code{2.0} Extreme \verb{-> 0}).
}
\code{stability_source} chooses which to use -- these are \emph{different harms} and can
diverge (HPRM's policy-substitution effect). \code{"edr"} (displacement only) is
the robust choice where EER is out-of-sample: HPRM's EER training excluded
California, so \code{ev_value} is out-of-sample there (flagged in
\code{eer_out_of_sample}). The attainable set entering the stability gate is the
chosen \code{availability} count; \code{stable_dest = attainable * stability}.
}
\examples{
\dontrun{
idx <- afford_index("53", "033", 2024, tenure = "rent")        # King County
sd  <- afford_stability(idx, hprm = "~/data/.../hprm_v5_full_2022.parquet")
# the stable-destination set for VLI renters (continuous, availability folded in):
sd[sd$ami_tier == "VLI", c("GEOID", "supply", "stability", "stable_dest", "stable_cat")]
# displacement-only stability (robust where EER is out-of-sample, e.g. CA):
afford_stability(idx, hprm = hprm_df, stability_source = "edr")
}
}
\seealso{
\code{\link[=afford_index]{afford_index()}}, \code{\link[=afford_bands]{afford_bands()}}
}
