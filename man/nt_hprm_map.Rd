% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_hprm.R
\name{nt_hprm_map}
\alias{nt_hprm_map}
\title{The HPRM jurisdiction map (the report-page embed)}
\usage{
nt_hprm_map(
  hprm,
  state = NULL,
  county = NULL,
  place = NULL,
  layers = c("edr", "eer", "hprm", "divergence"),
  year = 2020,
  erase_water = TRUE,
  declutter = TRUE,
  popup_note = NULL,
  opacity = 0.8,
  height = NULL,
  ...
)
}
\arguments{
\item{hprm}{Scored HPRM tract data: an \code{sf} object, or a plain data frame
to be joined onto \code{tigris} tract geometry via \code{state}/\code{county}/\code{year}.
Needs a \code{geoid}/\code{GEOID} column plus, per requested layer, \code{dis_risk2},
\code{ev_risk2}, \code{hprm_score}, \code{divergence}. Optional popup context columns
(used when present): \code{p_li}, \code{renters_e}, \code{nt_conc2}, \code{data_quality}
(a display-ready note, e.g. for tracts below the published map's
inclusion criteria).}

\item{state, county}{Passed to \code{\link[tigris:tracts]{tigris::tracts()}} when \code{hprm} has no
geometry (cartographic-boundary tracts, \code{cb = TRUE}).}

\item{place}{Optional jurisdiction name(s) (\code{\link[tigris:places]{tigris::places()}}): drawn as a
boundary line and used to frame the initial view. Incorporated places
(LSAD 25) are preferred when a name matches more than one polygon.}

\item{layers}{Which layers to add, in switcher order: any of \code{"edr"},
\code{"eer"}, \code{"hprm"}, \code{"divergence"} (default all four, EDR first — the
model's live signal in California).}

\item{year}{Geometry vintage for tracts/places (default 2020, matching the
hprm_v5 tract vintage).}

\item{erase_water}{Clip open water from tracts and boundary for display
(\code{\link[=nt_erase_water]{nt_erase_water()}}); the scores themselves are untouched. Default \code{TRUE}.}

\item{declutter}{Hide decorative basemap tints under the fills
(\code{\link[=nt_declutter_basemap]{nt_declutter_basemap()}}). Default \code{TRUE}.}

\item{popup_note}{Optional HTML/text appended to every popup — the seam for
region-specific caveats (e.g. California's EER suppression note). For a
per-tract note (e.g. the share of a straddling tract inside the framed
jurisdiction), put an \code{in_city_note} character column on \code{hprm}; it is
prepended to \code{popup_note} feature by feature.}

\item{opacity}{Fill opacity (default \code{0.8}).}

\item{height}{Optional map height in CSS pixels.}

\item{...}{Passed to \code{\link[=nt_add_choropleth]{nt_add_choropleth()}} (e.g. \code{legend}, \code{tooltip};
\code{before_id} defaults to \code{"water"} so fills sit under basemap water and
labels).}
}
\value{
A \code{maplibregl} htmlwidget — save with \code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}
and iframe it from the report page.
}
\description{
Renders scored HPRM tract data as the jurisdiction-report interactive map:
switchable choropleth layers for \strong{displacement risk (EDR)}, \strong{eviction
risk (EER)}, the \strong{HPRM composite (0-8)}, and \strong{convergence} (tracts
carrying both signals), each with its own legend, a rich per-tract popup,
and the jurisdiction boundary drawn on top with the view framed to it.
The surface is drawn county-wide so boundary effects stay visible — the
jurisdiction is framed, not clipped.

Colors come from \code{\link[=nt_hprm_pal]{nt_hprm_pal()}}, so the interactive map and any static
export read identically. Layer order in \code{layers} is switcher order; the
first layer is the one shown initially.
}
\examples{
\dontrun{
smc <- arrow::read_parquet("~/data/evictionresearch/hprm/hprm_v5_full_2022.parquet") |>
  dplyr::filter(substr(geoid, 1, 5) == "06081", map)
m <- nt_hprm_map(smc, state = "CA", county = "San Mateo",
                 place = "Redwood City",
                 popup_note = "California is excluded from the eviction-filing model; EER here is an extrapolation.")
htmlwidgets::saveWidget(m, "hprm_redwood_city.html", selfcontained = TRUE)
}
}
\seealso{
\code{\link[=nt_hprm_pal]{nt_hprm_pal()}}, \code{\link[=nt_add_choropleth]{nt_add_choropleth()}}, \code{\link[=nt_layers_control]{nt_layers_control()}}

Other ern_maps:
\code{\link[=afford_map]{afford_map()}},
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}},
\code{\link[=nt_add_labels]{nt_add_labels()}},
\code{\link[=nt_declutter_basemap]{nt_declutter_basemap()}},
\code{\link[=nt_hprm_pal]{nt_hprm_pal()}},
\code{\link[=nt_layers_control]{nt_layers_control()}},
\code{\link[=nt_map]{nt_map()}},
\code{\link[=nt_map_sync]{nt_map_sync()}},
\code{\link[=nt_maplibre]{nt_maplibre()}},
\code{\link[=nt_pmtiles]{nt_pmtiles()}},
\code{\link[=nt_tract_interpolate]{nt_tract_interpolate()}}
}
\concept{ern_maps}
