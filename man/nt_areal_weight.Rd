% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/areal_weight.R
\name{nt_areal_weight}
\alias{nt_areal_weight}
\title{Population-weighted areal crosswalk from census blocks to places}
\usage{
nt_areal_weight(
  state,
  county,
  places = NULL,
  year = 2020,
  lsad = "25",
  crs = 3310,
  ...
)
}
\arguments{
\item{state}{Two-letter state abbreviation or FIPS code. A single state per
call (passed to tigris).}

\item{county}{County name(s) or FIPS code(s) within \code{state}. May be a vector;
blocks are pulled for each and combined. \strong{Pass every county a target place
touches} — a place that spans a county not listed here keeps correct
fractions but silently omits its out-of-county tracts.}

\item{places}{Optional character vector of place \code{NAME}s to keep (for example
\code{c("Redwood City", "San Mateo")}). \code{NULL} (default) keeps every place of the
requested \code{lsad}. When supplied, each name must resolve to exactly one
polygon at the chosen \code{lsad}, so that downstream aggregation by \code{place_name}
is unambiguous; a name that resolves to several polygons is an error.}

\item{year}{Vintage for both blocks and places. Default \code{2020}. The block
schema — and therefore the population/housing counts — is detected from the
data: a \code{year >= 2020} pull carries 2020-decennial blocks (\verb{*20} columns),
\code{2010} carries 2010-decennial blocks (\verb{*10}). Counts are decennial-census
totals, not ACS estimates.}

\item{lsad}{Place LSAD code(s) to keep. Default \code{"25"} (incorporated places),
which excludes CDPs and unincorporated areas. Set \code{NULL} to keep all place
types — note a single \code{NAME} can then resolve to several polygons (e.g. a
city and a like-named CDP), which \verb{places=} will reject.}

\item{crs}{Projected CRS for the block-to-place point-in-polygon join. Default
\code{3310} (California Albers). The CRS affects only the spatial assignment of
blocks to places; because 2020 blocks nest within 2020 places, an interior
point lands in the same place under any sane projected CRS, so the returned
fractions are CRS-stable in practice. Outside California, pass a locally
appropriate projected (not lon/lat) CRS for a well-conditioned join.}

\item{...}{Passed to the underlying \code{\link[tigris:blocks]{tigris::blocks()}} call.}
}
\value{
A tibble with one row per (place, tract) overlap: \code{place_name},
\code{place_geoid}, \code{tract_geoid}, the in-place block totals (\code{pop_in}, \code{hu_in},
\code{area_in}), the whole-tract totals (\code{pop_t}, \code{hu_t}, \code{area_t}), and the
within-tract fractions inside the place (\code{frac_pop}, \code{frac_hu},
\code{frac_area}). Water / zero-population tracts return a fraction of \code{0} (no
divide-by-zero). Rows are sorted by place, then by descending \code{frac_pop},
for readability only — the fractions are independent of row order, so
aggregate rather than index positionally. With \verb{places=}, \code{place_name} is
unique per polygon; under \code{lsad = NULL} it may repeat.
}
\description{
Build an exact areal/population weight for how each census tract overlaps each
incorporated place (or other place type), using decennial census blocks as
the weighting surface. Because 2020 blocks nest within both 2020 tracts and
2020 places, the share of a tract's population (housing units, or land area)
that falls inside a place is computed \emph{exactly} — there is no
areal-interpolation approximation. Multiply any tract-level estimate
(HPRM scores, ACS counts, rates) by the returned \verb{frac_*} column to
population-weight it onto place boundaries.

Tracts straddle municipal limits, so a jurisdiction's profile is not simply
"the tracts inside the city." Using the \emph{incorporated} place boundary
(\code{lsad = "25"}) also drops census-designated places (CDPs) and unincorporated
pockets for free: their blocks fall outside every incorporated polygon and
therefore contribute zero weight.
}
\examples{
\dontrun{
# Share of each tract's population / renters inside San Mateo County's cities
w <- nt_areal_weight(
  "CA", "San Mateo",
  places = c("Redwood City", "San Mateo", "Brisbane", "San Bruno")
)

# Population-weight a tract-level estimate onto each city. `frac_pop` is the
# share of the tract's population inside the city, so renters_e * frac_pop is
# the tract's in-city renter households.
library(dplyr)
w \%>\%
  left_join(tract_estimates, by = c("tract_geoid" = "geoid")) \%>\%
  group_by(place_name) \%>\%
  summarise(renters_in_city = sum(renters_e * frac_pop, na.rm = TRUE))
}
}
\seealso{
\code{\link[=get_co_puma]{get_co_puma()}} for the PUMA-county crosswalk and \code{\link[=ntdf]{ntdf()}} for
tract-level neighborhood typologies.
}
