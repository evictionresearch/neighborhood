% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/afford_index.R
\name{ami_cutoffs}
\alias{ami_cutoffs}
\title{Area Median Income tier cutoffs by county}
\usage{
ami_cutoffs(
  state,
  counties,
  year = 2024,
  ami_source = c("acs", "hud", "hud_acs"),
  ami_tiers = c(ELI = 0.3, VLI = 0.5, LI = 0.8, MI = 1.2),
  hud_hh_size = 4
)
}
\arguments{
\item{state}{State (FIPS code like \code{"06"} or a name/abbreviation like \code{"CA"}).}

\item{counties}{County or counties (3-digit FIPS like \code{"075"} or names).}

\item{year}{ACS 5-year endpoint year.}

\item{ami_source}{One of \code{"acs"} (default; Census only), \code{"hud"} (official
published HUD limits via the HUD API; requires the \code{hudr} package and a
\code{HUD_API_KEY}), or \code{"hud_acs"} (full HUD-cascade reproduction; not yet
implemented). With \code{"hud"}, ELI/VLI/LI come straight from HUD's per-
household-size limits; tiers without a HUD limit (e.g. MI = 120\%) are
derived as a fraction of the area median.}

\item{ami_tiers}{Named numeric vector of tier fractions of AMI. Default
\code{c(ELI = .30, VLI = .50, LI = .80, MI = 1.20)}.}

\item{hud_hh_size}{Household size (1-8) for the HUD family-size adjustment;
default \code{4}. Use \code{NULL} to leave AMI unadjusted (4-person basis).}
}
\value{
A tibble with one row per county: \code{GEOID}, \code{NAME}, \code{ami}, and one
\verb{cut_<TIER>} income-ceiling column per tier.
}
\description{
Returns each study county's Area Median Income (AMI) and the income ceilings
for the standardized affordability tiers (ELI/VLI/LI/MI). In the default
\code{"acs"} source, AMI is the county median \strong{family} income (ACS \code{B19113},
HUD's anchor) and tiers are simple fractions of it, optionally adjusted for
household size with HUD's family-size factors.
}
\examples{
\dontrun{
ami_cutoffs("06", c("075", "081"), 2024)
}
}
\seealso{
\code{\link[=afford_index]{afford_index()}}
}
