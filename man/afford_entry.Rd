% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/afford_entry.R
\name{afford_entry}
\alias{afford_entry}
\title{Re-score affordability at entry prices (pane 2: availability)}
\usage{
afford_entry(
  x,
  zori,
  weights = NULL,
  burden = 0.3,
  stretch = 0.5,
  cover_min = 0.25
)
}
\arguments{
\item{x}{An \code{\link[=afford_index]{afford_index()}} result (any tiers; \code{rent} rows get entry
columns). Must carry \code{GEOID}, \code{county}, \code{year}, \code{ami_tier},
\code{income_cutoff}, \code{total}.}

\item{zori}{A frame from \code{\link[=afford_zori]{afford_zori()}} (or one with \code{zip} + \code{zori}).}

\item{weights}{A crosswalk from \code{\link[=nt_zcta_weights]{nt_zcta_weights()}} for the same counties
(\code{zcta}, \code{tract_geoid}, \code{frac_hu}). \code{NULL} builds it (downloads blocks +
the national ZCTA file on first use — slow once, cached after).}

\item{burden, stretch}{The two burden lines (defaults \code{0.30}/\code{0.50}) — match
what you passed to \code{\link[=afford_index]{afford_index()}}.}

\item{cover_min}{Minimum HU-share of a tract in ZORI-covered ZIPs before
the fallback premium is used (default \code{0.25}).}
}
\value{
\code{x} plus: \code{entry_premium}, \code{premium_coverage}, \code{premium_fallback},
\code{accessible_entry}, \code{accessible_entry_stretch}, \code{supply_entry},
\code{supply_entry_stretch}, \code{entry_verdict} (same three classes as
\code{\link[=afford_verdict]{afford_verdict()}}), and \code{available_entry_turnover} /
\code{available_entry_vacancy} when availability columns are present.
}
\description{
Pane 1 (\code{\link[=afford_index]{afford_index()}} + \code{\link[=afford_verdict]{afford_verdict()}}) prices each tract at what
\strong{current tenants} pay. \code{afford_entry()} re-scores it at what \strong{entering}
the tract costs: each tract gets an \strong{entry premium} (asking / standing
rent — Zillow ZORI over ACS ZIP standing rents, brought to tracts with
block-exact housing-unit weights), the rent distribution is shifted by it,
and the same 30\%/50\% verdict grammar is applied at the door price:
\code{entry_supply}, \code{entry_verdict}, and — when Gate-2 availability columns are
present — \code{available_entry_turnover}/\code{available_entry_vacancy} (units that
both open up \emph{and} are affordable at entry prices).
}
\details{
Mechanics: a unit is entry-affordable when \code{standing_rent * premium <= income_cutoff * burden / 12}, evaluated on the tract's \code{B25063} rent
brackets by interpolation (equivalently, the pane-1 cost line divided by
the premium). The premium mixes the true mover premium with drift between
the ACS 5-year window and the ZORI month — both push entry costs up, which
is exactly the correction pane 1 needs; disentangling them (ACS
recent-mover rents) is a documented validation step. Rental rows only
(\code{tenure == "rent"}); other tenures pass through with \code{NA} entry columns.
Tracts whose ZIPs lack ZORI coverage below \code{cover_min} (HU-share) fall back
to the study-area median premium and are flagged \code{premium_fallback}.

\strong{Not yet in this layer:} competition (an affordable unit occupied by a
higher-income household — the CHAS adjustment) and screening. Counts are
ceilings.
}
\examples{
\dontrun{
idx <- afford_index("53", "033", 2024, tenure = "rent", geometry = TRUE) |>
  afford_verdict()
ent <- afford_entry(idx, afford_zori())
afford_capacity(ent)                       # per-100 at standing vs entry
afford_map(ent, verdict_col = "entry_verdict")
}
}
\seealso{
\code{\link[=afford_zori]{afford_zori()}}, \code{\link[=nt_zcta_weights]{nt_zcta_weights()}}, \code{\link[=afford_capacity]{afford_capacity()}} (gains
per-100-at-entry columns), \code{\link[=afford_map]{afford_map()}} (\code{verdict_col = "entry_verdict"})
}
