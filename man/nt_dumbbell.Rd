% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_rank.R
\name{nt_dumbbell}
\alias{nt_dumbbell}
\title{Disparity dumbbell chart (ERN house style)}
\usage{
nt_dumbbell(
  data,
  x,
  low,
  high,
  low_label = NULL,
  high_label = NULL,
  sort = c("gap", "high", "low", "none"),
  n = NULL,
  orientation = c("horizontal", "vertical"),
  palette = NULL,
  value_fmt = c("comma", "percent", "currency", "multiple", "none"),
  value_labels = TRUE,
  legend = TRUE,
  yaxis = c("always", "hover", "none"),
  dot_size = 12,
  title = NULL,
  subtext = NULL,
  source = NULL,
  height = NULL,
  colors = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame: one row per category.}

\item{x}{Name of the category column.}

\item{low, high}{Names of the two value columns (the two ends of each
dumbbell). \code{high} is the focus/right end (accented by default), \code{low} the
comparison/left end.}

\item{low_label, high_label}{Series names for the two ends, shown in the
legend, e.g. \code{"White"} and \code{"Black"}. (The tooltip shows the category and
the value.)}

\item{sort}{\code{"gap"} (default; by \code{high - low}), \code{"high"}, \code{"low"}, or
\code{"none"}.}

\item{n}{Optional integer; keep only the top \code{n} categories after sorting.}

\item{orientation}{\code{"horizontal"} (default) or \code{"vertical"}.}

\item{palette}{Length-2 colors for the \code{low} and \code{high} ends (defaults to a
muted steel and the ERN accent). (\code{colors} is a deprecated alias.)}

\item{value_fmt}{Value formatting (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{value_labels}{Logical; label each end with its value (default \code{TRUE}).}

\item{legend}{Logical; show the two-end legend (default \code{TRUE}).}

\item{yaxis}{Value-axis scale visibility (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{dot_size}{Dot diameter in pixels (default \code{12}).}

\item{title, subtext, source, height}{Standard editorial extras (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{colors}{Deprecated alias for \code{palette}.}

\item{...}{Passed to \code{\link[echarts4r:e_scatter]{echarts4r::e_scatter()}}.}
}
\value{
An \code{echarts4r}/\code{htmlwidget} object.
}
\description{
Two dots per category joined by a connecting bar — the cleanest way to show a
\emph{gap} between two measures, which is the Eviction Research Network's core
visual idiom for racial disparities (e.g. the Black vs. white filing rate by
county) and for change between two periods (2019 vs. 2024). Categories are
sorted by the size of the gap so the widest disparities rise to the top.
}
\examples{
\dontshow{if (interactive() && requireNamespace("echarts4r", quietly = TRUE)) withAutoprint(\{ # examplesIf}
gap <- data.frame(
  county = c("Hennepin", "Ramsey", "Dakota", "Anoka"),
  white  = c(12, 14, 9, 8),
  black  = c(41, 38, 22, 19)
)
nt_dumbbell(gap, "county", low = "white", high = "black",
            low_label = "White", high_label = "Black")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_lollipop]{nt_lollipop()}} for a single-value ranked dot plot.

Other ern_charts:
\code{\link[=ern_palette]{ern_palette()}},
\code{\link[=ern_swatch]{ern_swatch()}},
\code{\link[=nt_bar]{nt_bar()}},
\code{\link[=nt_chart]{nt_chart()}},
\code{\link[=nt_lollipop]{nt_lollipop()}},
\code{\link[=nt_pd_bart]{nt_pd_bart()}},
\code{\link[=nt_pd_plot]{nt_pd_plot()}},
\code{\link[=nt_range]{nt_range()}},
\code{\link[=nt_scatter]{nt_scatter()}},
\code{\link[=nt_slope]{nt_slope()}},
\code{\link[=nt_spark]{nt_spark()}},
\code{\link[=nt_stacked_bar]{nt_stacked_bar()}},
\code{\link[=nt_waffle]{nt_waffle()}},
\code{\link{scale_ern}},
\code{\link[=theme_ern]{theme_ern()}}
}
\concept{ern_charts}
