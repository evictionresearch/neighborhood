% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_stacked_bar.R
\name{nt_stacked_bar}
\alias{nt_stacked_bar}
\title{Composition (stacked / 100\%) bar chart (ERN house style)}
\usage{
nt_stacked_bar(
  data,
  x,
  y,
  group,
  proportional = TRUE,
  orientation = c("horizontal", "vertical"),
  palette = NULL,
  value_labels = TRUE,
  label_min = 0.05,
  value_fmt = c("comma", "percent", "currency", "multiple", "none"),
  yaxis = c("always", "hover", "none"),
  legend = TRUE,
  title = NULL,
  subtext = NULL,
  source = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame in long form: one row per \code{x} × \code{group}.}

\item{x}{Name of the category column (one stacked bar per value).}

\item{y}{Name of the numeric value column (counts or amounts).}

\item{group}{Name of the column whose levels stack within each bar.}

\item{proportional}{Logical; normalize each bar to 100\% (default \code{TRUE}).}

\item{orientation}{\code{"horizontal"} (default) or \code{"vertical"}.}

\item{palette}{Segment colors, one per \code{group} level. Defaults to a neutral
accent-free ramp (interpolated to the number of groups; the brand accent is
reserved for a single highlight, not a whole stacked band). A named vector
is matched to group names — recommended for race compositions (e.g.
\code{c(Black = "#F9322B", White = "#aab4be", ...)}).}

\item{value_labels}{Logical; print each segment's share/value inside it
(default \code{TRUE}).}

\item{label_min}{Minimum segment share (of its bar) to label, so slivers stay
uncluttered (default \code{0.05}). Applies when \code{proportional = TRUE}.}

\item{value_fmt}{One of \code{"comma"} (default), \code{"percent"}, \code{"currency"},
\code{"multiple"}, or \code{"none"}; applies when \code{proportional = FALSE} (ignored when
proportional — segments are always shown as percentages).}

\item{yaxis}{Value-axis scale visibility (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{legend}{Logical; show the group legend (default \code{TRUE}).}

\item{title, subtext, source, height}{Standard editorial extras (see \code{\link[=nt_bar]{nt_bar()}}).}

\item{...}{Passed to \code{\link[echarts4r:e_bar]{echarts4r::e_bar()}}.}
}
\value{
An \code{echarts4r}/\code{htmlwidget} object.
}
\description{
A stacked bar chart for showing \emph{composition} — what share each \code{group} makes
up within each category \code{x}. By default it normalizes each bar to 100\%
(\code{proportional = TRUE}), the form used across the CiDR Lab jurisdiction
reports for tenure-by-race, rent-burden-by-AMI, and racial composition. Set
\code{proportional = FALSE} for raw stacked counts. Segment shares are labelled
inside the bars, and the chart is horizontal by default.
}
\examples{
\dontshow{if (interactive() && requireNamespace("echarts4r", quietly = TRUE)) withAutoprint(\{ # examplesIf}
tenure <- data.frame(
  race   = rep(c("White", "Black", "Latine", "Asian"), each = 2),
  tenure = rep(c("Own", "Rent"), 4),
  hh     = c(620, 380, 240, 760, 300, 700, 540, 460)
)
nt_stacked_bar(tenure, "race", "hh", group = "tenure")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_bar]{nt_bar()}} for single-value bars.

Other ern_charts:
\code{\link[=ern_palette]{ern_palette()}},
\code{\link[=ern_swatch]{ern_swatch()}},
\code{\link[=nt_bar]{nt_bar()}},
\code{\link[=nt_chart]{nt_chart()}},
\code{\link[=nt_dumbbell]{nt_dumbbell()}},
\code{\link[=nt_lollipop]{nt_lollipop()}},
\code{\link[=nt_pd_bart]{nt_pd_bart()}},
\code{\link[=nt_pd_plot]{nt_pd_plot()}},
\code{\link[=nt_range]{nt_range()}},
\code{\link[=nt_scatter]{nt_scatter()}},
\code{\link[=nt_slope]{nt_slope()}},
\code{\link[=nt_spark]{nt_spark()}},
\code{\link[=nt_waffle]{nt_waffle()}},
\code{\link{scale_ern}},
\code{\link[=theme_ern]{theme_ern()}}
}
\concept{ern_charts}
