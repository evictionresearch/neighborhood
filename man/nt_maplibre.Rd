% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nt_maplibre.R
\name{nt_maplibre}
\alias{nt_maplibre}
\title{Start an ERN-styled MapLibre map}
\usage{
nt_maplibre(
  data = NULL,
  style = "positron",
  controls = TRUE,
  search = TRUE,
  fit = TRUE,
  center = NULL,
  zoom = NULL,
  projection = "mercator",
  ...
)
}
\arguments{
\item{data}{Optional spatial data to frame the initial view: an \code{sf} object,
or a path to a shapefile / GeoJSON / GeoPackage, an \code{sfc}, or an \code{sp}/\code{terra}
object (all normalized to \code{sf} and re-projected to WGS84 as needed). If
supplied and \code{fit = TRUE}, the map opens framed to its bounds. (Data is not
drawn here — add it with \code{\link[=nt_add_choropleth]{nt_add_choropleth()}}.)}

\item{style}{Basemap. One of the OpenFreeMap style names \code{"positron"}
(default), \code{"bright"}, \code{"liberty"}, \code{"dark"}, or \code{"fiord"}, \strong{or} any value
\code{mapgl} accepts (a style URL, or the result of \code{\link[mapgl:carto_style]{mapgl::carto_style()}},
\code{\link[mapgl:maptiler_style]{mapgl::maptiler_style()}}, etc.).}

\item{controls}{Logical; add the standard control set (default \code{TRUE}).}

\item{search}{Logical; include an OpenStreetMap/Nominatim address search box
(default \code{TRUE}, no API key required). Ignored when \code{controls = FALSE}.}

\item{fit}{Logical; if \code{data} is supplied, fit the initial view to it
(default \code{TRUE}). Ignored if \code{center} is given.}

\item{center}{Optional \code{c(lng, lat)} to center the map instead of fitting.}

\item{zoom}{Optional initial zoom (used with \code{center}).}

\item{projection}{Map projection; defaults to \code{"mercator"} (the flat web map
look of the state-profile maps). Pass \code{"globe"} for the 3-D globe.}

\item{...}{Passed to \code{\link[mapgl:maplibre]{mapgl::maplibre()}} (e.g. \code{height}, \code{bearing}, \code{pitch}).}
}
\value{
A \code{mapgl}/\code{maplibregl} \code{htmlwidget}.
}
\description{
Initializes an interactive \href{https://maplibre.org/}{MapLibre GL JS} map with
the Eviction Research Network house style: an OpenFreeMap basemap, a flat
(web-mercator) projection, the standard control set (zoom/compass,
fullscreen, geolocate, and an address search box), and — if you hand it
spatial data — an automatic fit to that data's bounds.

This is the foundation the other \verb{nt_*} map functions build on. It returns an
ordinary \code{mapgl} map object, so you can keep building with \code{\link[=nt_add_choropleth]{nt_add_choropleth()}}
and \code{\link[=nt_add_labels]{nt_add_labels()}} \strong{or} drop straight into any \code{mapgl} function (e.g.
\code{\link[mapgl:add_line_layer]{mapgl::add_line_layer()}}, \code{\link[mapgl:add_layers_control]{mapgl::add_layers_control()}}) at any point in the
pipe. The neighborhood wrappers never lock you in.

Maps are \code{htmlwidgets}: they display in RStudio, Quarto/R Markdown, and Shiny,
and can be written to a self-contained HTML file with
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}} — the same delivery format as the HPRM, Minnesota,
and Washington state-profile maps.
}
\examples{
\dontshow{if (interactive() && requireNamespace("mapgl", quietly = TRUE)) withAutoprint(\{ # examplesIf}
md <- ntdf(state = "MD", county = "Baltimore City", geometry = TRUE)
nt_maplibre(md) |>
  nt_add_choropleth(md, "nt_conc")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}}, \code{\link[=nt_add_labels]{nt_add_labels()}}, \code{\link[=nt_map]{nt_map()}};
\code{\link[mapgl:maplibre]{mapgl::maplibre()}} and \code{\link[mapgl:openfreemap_style]{mapgl::openfreemap_style()}} for the underlying
engine by Kyle Walker.

Other ern_maps:
\code{\link[=afford_map]{afford_map()}},
\code{\link[=nt_add_choropleth]{nt_add_choropleth()}},
\code{\link[=nt_add_labels]{nt_add_labels()}},
\code{\link[=nt_declutter_basemap]{nt_declutter_basemap()}},
\code{\link[=nt_layers_control]{nt_layers_control()}},
\code{\link[=nt_map]{nt_map()}},
\code{\link[=nt_map_sync]{nt_map_sync()}},
\code{\link[=nt_pmtiles]{nt_pmtiles()}}
}
\concept{ern_maps}
